# Public values only. Never put tunnel credentials or private keys in VITE_*.
# Copy to .env.web.local, then: npm run build -- --mode web
VITE_STUDIO_MODE=web
VITE_API_ORIGIN=https://api.opendpd.com
VITE_BASE_PATH=./
