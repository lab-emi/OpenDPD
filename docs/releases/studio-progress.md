# OpenDPD Studio — stage progress

Per-stage status against the acceptance criteria of
`OpenDPD_Studio_Development_Plan.md`. "done" means evidence exists in this
repository; "pending human" means a maintainer decision is required;
"not verified" means the environment could not produce the evidence.

| Stage | Status | Commit | Notes |
|---|---|---|---|
| S00 baseline & governance | done except maintainer approvals | `6f026b1` | `docs/baseline/baseline-report.md` |
| S01 contracts & UX | done except prototype (delivered with S05) | `0f66ad0` | schemas, ADR-0001, UX spec, tokens, mock examples |
| S02 explicit config, registry, workspace | done | `9ea598c` | `opendpd run/validate/models/recipes/datasets`, registry, resolver, adapter, workspace |
| S03 task runtime | done (Windows/macOS cleanup not verified) | `fd63107` | SQLite store, supervisor, worker subprocess, cancel, recovery |
| S04 local service & API | done | `076de58` | FastAPI app, sessions/CSRF/Host checks, SSE replay, OpenAPI contract, threat model |
| S05 React workbench | done (visual baselines local only) | `c002ca2` + `c5f5b83` | `frontend/`: pages, domain components, states, Vitest + Playwright journeys, generated API types |
| S06 packaging & one-command launch | done on Linux; **macOS/Windows not verified** | `86ddd7a` | `opendpd gui`, `opendpd doctor`, wheel/sdist carry the built frontend, packaged L2 test |
| S07 data import, Dataset Doctor, traceable preprocessing | done | `4fea772` + `dd85377` | import roots + upload, `dataset-doctor-v1`, `preprocess-v1` versions, `contiguous-v1` split, datasets pages, J2 journey (mock) and headless CLI J2 on real data |
| S08 versioned metrics & evaluation evidence | done | `999f031` + `51891cc` | `opendpd/core/metrics` registry (`legacy-opendpd-v1` frozen, `general-spectral-v1`), analytic reference tests, results re-scored from the best checkpoint under every profile, profile switcher and definitions in the GUI |
| S09 PA training closure & experiment management | done except the human-gated items below | `370616e` + `a6f68c5` | registry-generated model parameters, config import/export/re-run, device refusal at validation, GUI-vs-CLI consistency test, real-browser PA training on imported data |
| S10 DPD training, signal generation & surrogate evaluation | done | `e3f3d6a` + `1d061e7` | explicit x/u/y chain in every DPD result, baselines under one reference, surrogate amplitude coverage, `run_dpd` results through any surrogate (never overwriting), export sidecar + reload test, lineage route/card, `opendpd apply`, Apply-DPD dialog |
| S11 result comparison, visualisation & reproducible packages | done except the G1 human-gated item below | `abaf8af` + `50f5956` + `281b93a` | plots-v1 data with fixed budgets, per-epoch history, comparison under one profile with explicit incompatibilities (incl. PA surrogate, reference gain, execution semantics), full/share packages with hash-verified import, reports bound to stored results, `opendpd export/import/report`, Export panel + Import package in the GUI |
| S12 benchmark, classical baselines & numerical regression | done except the human-gated items below | `5fa2026` + `1d0083a` | least-squares MP/GMP baselines as registry models with recorded fit stability and stated training paths; `benchmark-v1`: pre-registered plans (≥ 3 seeds, fixed budgets), idempotent execution, hash-bound per-seed reports with a data audit, regression baselines that block only once approved, `opendpd benchmark plan/run/report/check/baseline`, cpu tier in the weekly workflow, draft baseline for the built-in data |
| S13 performance, security, accessibility & cross-platform hardening | done except the human-gated items below | `68953a1` (+ the S14 commit for the performance report) | CSP and security headers, body/upload caps without `Content-Length`, bounded package extraction, one restricted checkpoint loader, device-busy classification; server-side run search/paging, windowed logs, memory-mapped import, bounded doctor window; axe-core + keyboard journeys, dash/marker encodings; Chromium/Firefox/WebKit projects; packaged train/export/exit test; workspace migration and recovery tests; performance harness and report |
| S14 external trial, documentation, contribution flow & first public version | internal items done; trial, onboarding, desktop checks and publication **pending human** | (this stage's commit) | documented commands executed in CI with a GUI-exported configuration through the CLI, network-isolation test, tutorials (`adding-a-model`), release notes, external-trial protocol, prioritised backlog; performance findings closed: `execution.num_threads` applied by the executor, run timestamps stamped by the executor on every path |
| S15 limited-scope Python waveform & standard evaluation profile | implemented and tested; **pending science review, cross-validation and error-budget approval** | (this stage's commit) | `ofdm-lte20-v1` reference waveform (CP-OFDM, LTE 20 MHz numerology, known 64QAM symbols) regenerated from its spec; reference-vector packages (`opendpd waveforms generate/show`); dataset binding by correlation (`datasets import --waveform`); `ofdm-lte20-evm-v1` data-aided EVM + E-UTRA-style ACLR with explicit statuses; `MetricProfile.validation` gates GUI visibility; protocol with deviations, error budget and cross-validation procedure |

## S00 acceptance items

| Item | Status |
|---|---|
| `baseline-report.md` with commands, env, results, failure causes | done |
| CLI/API compatibility cases, metric goldens, legacy checkpoint load case registered | done (`tests/golden`) |
| Maintainer approval of G0–G2 scope, support matrix, risks, thresholds | **pending human** |
| Protected paths cannot be changed by ordinary PRs | done (`protected-paths.yml`, CODEOWNERS) |
| Agent scope, budget, max pending PRs, stop conditions configured | done (`AGENTS.md`) |

## S01 acceptance items

| Item | Status |
|---|---|
| Three journeys with success / failure / exit paths | done (`docs/architecture/ux-spec.md`) |
| Page structure reviewed by maintainer; advanced params collapsed | spec written; **pending human review** |
| Every result expresses `pa_modeling` / `dpd_surrogate` / `dpd_measured` with source and profile version | done (`EvaluationResult` validators) |
| Schema examples: missing metadata, N/A metric, interrupted run, log disconnect, legacy import | done (`opendpd/schemas/examples.py`, 49 tests) |
| Frontend prototype on fixed, labelled mock | deferred to S05 (mock fixtures are generated from the same examples) |

## S02 acceptance items

| Item | Status |
|---|---|
| Two experiments in sequence do not leak configuration; parallel workers do not share output files | done (`tests/integration/test_cli_run.py::test_sequential_runs_do_not_leak_configuration`; each run has its own directory) |
| GUI / CLI / Python API normalise identically; defaults have one source | done (`opendpd.services.config.resolve`; `test_training_defaults_have_one_source`; legacy-CLI numeric equality test) |
| Model list from the registry; CLI choices and GUI options not hand-written twice; unknown models cannot be submitted | done for the new path (`opendpd models`, `test_registry_covers_every_working_legacy_backbone`); the legacy parser keeps its own list for compatibility |
| MP/GMP-style non-neural methods can express their own fitting logic | done (S12): `mp_ls` / `gmp_ls` are registry models with `training_method: least_squares`; `opendpd/core/polynomial.py` fits them (column-normalised truncated SVD, diagnostics recorded) and `opendpd/services/polynomial.py` runs them as ordinary runs (PA: direct least squares; DPD: indirect learning on the measured train split, scored through a gradient-trained surrogate); the result states the training path |
| Legacy commands and public API compatibility tests pass; deprecations announced | done (existing suite passes; nothing deprecated yet) |
| Plain `pip install opendpd` needs no GUI dependency; runs work from a read-only install into an external workspace | done (`tests/packaging/test_wheel_install.py`, `tests/unit/test_lazy_imports.py`) |

## S03 acceptance items

| Item | Status |
|---|---|
| Same idempotency key never creates a duplicate; each run has its own directory | done (`test_run_completes_through_worker_with_events`, DB unique index) |
| API stays responsive while a run trains; queued runs and cancel handled | done (`test_api_stays_responsive_and_queue_is_serial`) |
| Run visible after closing/reopening the browser; events resume from the last seq | store-level done (`events_after`); browser-level verified in S04/S05 |
| Killed worker, OOM, disk write failure, config error give explicit terminal states | done: SIGKILL → `failed/worker_died` with OOM hint; unwritable run dir → `failed`; invalid config → rejected before a run exists |
| Service restart marks unfinished runs `interrupted`; no ghost running runs | done (`test_restart_recovery_marks_interrupted`, orphan terminated) |
| Cancel/exit leave no child processes; identity is pid + creation time | done on Linux; **Windows/macOS not verified** |
| No pause/resume button; checkpoint resume not offered | done (not implemented, not shown) |

## S04 acceptance items

| Item | Status |
|---|---|
| Browser refresh / reopen shows running run and continues logs & metrics without loss | API level done: snapshot + `events/list?after=` + SSE `Last-Event-ID` (`test_studio_api.py::test_run_lifecycle_events_logs_artifacts_result`); browser level in S05 |
| Foreign web pages cannot call the local API; DNS rebinding refused | done (`test_cross_origin_and_csrf`, `test_host_header_enforced`; threat model) |
| Paths from clients cannot escape the workspace | done (artifact download by registered id only; `test_artifact_download_by_id_only`) |
| Error responses are structured, field-level, with hints | done (`error.code/message/details[]/hint`; `test_validate_reports_structured_errors_without_starting`) |
| OpenAPI is the single contract; TypeScript types generated and checked in CI | contract committed and checked (`scripts/export_openapi.py --check` in CI); TypeScript generation lands with the frontend in S05 |
| No request causes training inside the server process | done (all runs go through the S03 supervisor; `test_cancel_through_api` measures cancel latency < 1 s while a worker trains) |

## S05 acceptance items

| Item | Status |
|---|---|
| TypeScript strict, lint, unit and interaction tests pass without `any` / disabled rules | done (`npm run typecheck`, `npm run lint`, 21 Vitest tests; CI job `frontend`) |
| Main forms usable by keyboard; required fields, errors and focus explicit; colour never the only encoding | done (`NewExperimentPage.test.tsx` keyboard-only submit; server errors on the field with `aria-invalid`; `StatusChip.test.tsx` label + icon) |
| 1366×768 and 1920×1080 layouts without horizontal scroll; charts enlargeable | done (`e2e/journey.spec.ts` "fit" test on both viewports; enlarge dialog) |
| Unconfigured, running, failed and disconnected states have an actionable next step | done (`StateBlock`, `RunDetailPage` next-step banners, `RunDetailPage.test.tsx` disconnected and failed cases) |
| Run detail refreshable and addressable; refresh never resubmits | done (`/runs/:runId`; e2e reload keeps the run and the fake API records one submission) |
| One real-size visualisation prototype with performance evidence | done (component gallery: PSD 2560 bins × 3 traces + I/Q window of 20 000 samples; probe asserts < 2 s, measured ≈ 210 ms per chart in headless Chromium) |
| Frontend prototype on fixed, labelled mock (carried over from S01) | done (`/gallery` and `ResultView` on `frontend/mocks/*`, every mock result carries the MOCK badge) |
| Visual regression baseline | Playwright screenshots of the gallery captured on Linux/Chromium and committed; **compared locally only** (CI runs `--ignore-snapshots` because font rendering differs) |

## S06 acceptance items

| Item | Status |
|---|---|
| Wheel installed outside the repo without Node.js; one command opens a usable GUI | done on Linux (`test_wheel_ships_gui_and_one_command_serves_it`: fresh venv, `opendpd gui --no-browser`, healthy → ready → page served → clean exit) |
| sdist also carries usable static assets, or generates them in an explicit build stage | done: `MANIFEST.in` includes `opendpd/studio/static`; CI `build` job builds the frontend before `python -m build` and asserts both artifacts contain it; a source checkout without the build shows a diagnostic page and `opendpd doctor` names the fix |
| Offline after install: example, fonts, icons, charts, training and export work | fonts are system stacks, icons are bundled SVG, Plotly is bundled; `tests/unit/test_offline_assets.py` asserts no external loads in the built assets. Export is S11 |
| Port in use, no default browser, no desktop, duplicate start, paths with spaces/Chinese | done: explicit busy port → exit 2 with message; auto-port skips busy ports; `webbrowser` failure never aborts (URL printed); second start reuses the running instance via `.studio.lock`; tests use workspaces named with spaces and Chinese characters |
| Browser opens only after the health check; missing/mismatched static shows a diagnostic, never a blank page | done (`test_browser_opens_only_after_health_check_with_bootstrap_url`; `/readyz` reports frontend presence and version; SPA route returns the diagnostic page when assets are missing or mismatched) |
| Real browser launch on three platforms | Linux: verified 2026-09-06 in a real desktop browser against `opendpd gui` (bootstrap URL → register example → new experiment validated server-side → 3-epoch run with live metrics → result with evidence badge); the first real pass found and fixed two defects (`evaluation` was required in the contract; a port left in TIME_WAIT was reported busy). **macOS and Windows: pending human** |
| Access session and file boundary active; no "open all local files for the demo" | done (S04 boundary unchanged; artifacts by id only) |

## S07 acceptance items

| Item | Status |
|---|---|
| Built-in data and user CSV import; wrong column names, lengths, I/Q order and units can be explained and corrected | done: `POST /datasets/inspect` returns headers, a preview, problems and a suggested mapping (alias table incl. `tx_i`/`rx_q`); the GUI mapping selects and the CLI `--map LOGICAL=COLUMN` fix swapped I/Q; length/dtype/object-array problems are reported before anything is written (`tests/unit/test_datasets_service.py`) |
| Known delay, gain, outliers and clipping detected within the fixture protocol; natural PA non-linearity is not called "broken" | done: `tests/fixtures/manifest.json["doctor_protocol"]` (delay ±0.1 sample at 3/7/40, gain ±0.5 dB / ±3°, clipping at 60 % of peak, 5 spikes, NaN blocks); `tests/unit/test_doctor.py` (10 tests incl. clean memory-polynomial PA raising no defect) |
| error/warning/info with evidence and suggestions; insufficient metadata blocks unreliable evaluation | done: every item carries `evidence`; `metadata_missing`, NaN, length, silent signal are blocking and set `evaluation_blocked` (`docs/protocols/dataset-doctor.md`) |
| Preview before a new version is created on confirmation; raw hash unchanged; parameters and code version recorded | done: `POST …/preprocess/preview` then `POST …/preprocess` with a version name; `versions/<name>/version.json` stores params, `code_version=preprocess-v1`, `fit_range`, steps; `raw/` is hashed and never modified (`test_versions_keep_raw_untouched_and_fit_only_on_train`) |
| Split before framing; boundary isolation covers the context | done: `contiguous-v1` splits the continuous signal with a guard (default 256 samples) before any framing (`opendpd/core/splits.py`, protected path); validation warns when `training.frame_length` exceeds the guard (`test_validate_names_missing_versions_and_guard_shorter_than_the_frame`). Built-in/legacy directories keep their original split (guard 0, no warning) |
| Fit range of learned preprocessing recorded; never fitted on the test set | done: `normalize=peak_input` requires `fit_range` = the training split and refuses otherwise; measurement alignment (delay/gain) is a separate, declared correction |
| Large files read incrementally; browser upload and authorised-directory import are distinct; no arbitrary path browsing | done: CSV read in 200k-row chunks, NumPy via memory-mapped `np.load(mmap_mode="r", allow_pickle=False)`; upload streams into `imports/uploads/` (2 GB cap, other bodies 2 MB); import only by `(root_id, relative path)` with traversal refused (`test_roots_listing_and_traversal_refused`) |
| GUI, CLI and Python API return the same diagnostic report and manifest | done: one service (`opendpd.services.datasets`) behind `opendpd datasets import/doctor/preprocess` and the routes; `test_inspect_import_doctor_preprocess_flow` compares the CLI JSON report with the API one |
| Real-data end-to-end through GUI/CLI consistency (G1 gate, partial) | CLI: `test_cli_import_doctor_preprocess_and_train_on_a_version` imports a synthetic CSV, runs the doctor, creates `aligned-v1`, trains on it and gets a result. GUI: J2 journey runs against the mock API (`frontend/e2e`); a real-browser pass on real data is a G1 checklist item, not yet done |

## S08 acceptance items

| Item | Status |
|---|---|
| Historical profile keeps its old numbers on frozen fixtures; fixes need a new version or explicit compatibility mode | done: `legacy-opendpd-v1` is `frozen=True`; `tests/golden/test_legacy_metrics_golden.py` (rel/abs 1e-7) and `test_legacy_profile_through_the_registry_reproduces_the_frozen_goldens` pin it; `opendpd/core/metrics/` is a protected path |
| Analytic / independent reference tests: zero error, known complex gain error, known noise power, known band power | done (`tests/unit/test_metrics_general.py`): zero error → documented −300 dB floor; gain g → exactly 20·log10\|g−1\|; white noise → SNR ±0.1 dB pooled and SNR+10·log10(BW/fs) ±0.15 dB in band; adjacent tone → its power ratio ±0.05 dBc |
| Negative-dBc leakage vs positive-dB suppression kept apart; name, formula and better-direction consistent | done: `ACPR_L/R` are leakage (adjacent/main, dBc, lower is better); the suppression convention is documented as −ACPR and not reported; the legacy `ACLR_*` notes state their convention; `MetricDefinition` carries display name, formula, aggregation, unit and direction and the GUI renders them from the registry |
| Pooled NMSE vs mean of per-segment dB; EVM ratio / percent / dB not mixed | done: `general` NMSE is pooled, `legacy` NMSE is the mean of segment dB (`test_pooled_nmse_is_not_the_mean_of_per_segment_db`); `IBE` is a dB power ratio with the ratio and percent forms documented in its notes; the legacy EVM is labelled repo-specific and "not a demodulated EVM" |
| No valid reference, zero reference energy, band outside capture, empty band, non-finite → explicit statuses, never a fake 0 or silent NaN | done: `test_every_undefined_case_has_an_explicit_status` covers all six cases; `MetricValue` forbids a value without `ok` and `ok` without a finite value |
| GUI shows evidence label and metric profile by default; surrogate results never displayed as measured | done: `EvidenceBadge` + profile chip on every result, the profile switcher lists the stored profiles, `MetricCard` shows the registry display name and a definition tooltip (`ResultDetailPage.test.tsx`, e2e J1); evidence type is declared by the worker, never inferred in the GUI |
| Display decimation, zoom or chart filtering never change formal metrics | done by construction: metrics are computed once in the worker from the best checkpoint over the full valid range and stored (`results/<profile>.json`); charts (S11) come from separate decimated data. Documented in `docs/protocols/metric-profiles.md` |
| No automatic ranking across incompatible protocols; side-by-side allowed with the reason | done at the core level: `opendpd.core.metrics.incompatibilities(a, b)` names profile/version, evidence type, dataset, preprocessing version, split protocol, split, reference kind and mock status (`test_results_under_different_protocols_are_not_comparable`); the comparison page (S11) must use it |
| Results carry data, model, preprocessing, reference and evaluation source | done: `EvaluationResult` (dataset evidence incl. preprocessing/split versions and valid range, model evidence with weight hashes, reference kind and gain rule, evidence type, profile id/version); GUI / CLI (`opendpd evaluate`, `opendpd profiles`) / Python API (`opendpd.services.evaluation`) share one implementation |

## S09 acceptance items

| Item | Status |
|---|---|
| After importing data, a real PA training through the GUI produces a valid checkpoint, logs and an evaluation result | done: verified 2026-09-06 in the desktop browser against `opendpd gui` on a scratch workspace: `imports/lab/capture.csv` (40 000 synthetic samples, +6 samples delay, 3 spikes) → Import my data (mapping guessed, metadata typed) → Dataset Doctor found the delay (+6.04), the 3 spikes and the +0.31 dB gain → Preprocess with the doctor's estimates, preview (40 000 → 39 992, aligned +0.00) → `aligned-v1` → New experiment on `aligned-v1` → 3-epoch GRU run succeeded with live per-epoch metrics → result page: PA-model evidence badge, `legacy-opendpd-v1 v1 · frozen` chip, NMSE −23.61 dB, switchable to `general-spectral-v1` (pooled NMSE −24.49 dB, IBE, ACPR). On disk: `save/…/*.pt`, history/best logs, `result.json`, `results/{legacy,general}.json`, provenance naming `aligned-v1` |
| Data, device, model parameters and paths are checked before submission; unsupported devices are refused, never switched silently | done: `experiments.submission_issues` (dataset version, split guard vs frame context, device detected) runs in `POST /experiments/validate`, `POST /runs` and `opendpd run`; `test_undetected_device_is_refused_never_switched` (422 `invalid_config` on `execution.device` with the hint to choose cpu or a detected device); model parameters are validated by the registry and reported per field |
| Quick demos and formal training are clearly distinct; short runs are never dressed up as benchmarks | done: recipes carry `purpose` (smoke / research), the form shows the smoke banner, the resolver warns on `training.epochs`, and every result of a short run lists the limitation (`SMOKE_EPOCH_LIMIT`) |
| Same CPU deterministic fixture through GUI and CLI: identical resolved configuration, outputs within the frozen tolerance | done: `tests/integration/test_entry_consistency.py` runs one configuration (`reproducibility: hard`, 2 epochs) through the HTTP API + worker subprocess and through `opendpd run` in-process: same `config_sha256`, same resolved configuration, same selected epoch, every metric within 1e-3 dB (tolerance recorded in `docs/protocols/acceptance-thresholds.md`, **pending maintainer approval**) |
| Checkpoint selection uses the configured validation metric, never the test split | done: the resolver fixes `evaluation.checkpoint_selection_metric` per task protocol (`test_config_resolve.py`), the legacy trainer selects on validation, and `test_result_metrics_come_from_the_registry_and_agree_with_the_training_log` asserts `selected_epoch` = argmin `VAL_NMSE` of the history log |
| Failure, cancel, busy device, queue wait and missing checkpoint have real states; no invented progress | done for failure/cancel/queue/missing checkpoint (S03: `failed/worker_died`, `cancelled`, `queued`, `input_missing`; progress comes only from epoch events); an unavailable device is refused before a run exists (above). A *busy* accelerator (another process holding it) cannot be told apart from an out-of-memory refusal in general; since S13 both are classified as `device_busy_or_out_of_memory` with a hint to check the device and retry, and the run is never retried or moved silently |
| Logs paged / virtualised, errors located to a stage, no sensitive paths or credentials in outward diagnostic packages | logs: done (byte-offset paging, `LogViewer`); errors: done (`RunError.stage` = prepare / train / evaluate / finalize); outward packages: done (S11 share packages redact user data, logs and machine paths and list what was removed; S13 negative tests cover traversal, symlink, bomb and flood members) |
| Reference recipes, registry-generated parameters, experiment list, curves, logs, checkpoints, config comparison, config import/export | done: recipes (S02), advanced settings generated from `ModelInfo.params` (name, type, limits, description; `NewExperimentPage.test.tsx`), experiments list + run detail with per-epoch curves / logs / artifacts / resolved config (S05), `ConfigDiff` component, **Download configuration (JSON)** and **Re-run with this configuration** on the run page, **Import configuration…** on the form (resolution block dropped, submitted as is) |

## S10 acceptance items

| Item | Status |
|---|---|
| Without a compatible PA surrogate a DPD recipe cannot start, and the remedy is stated | done: `bind_references` refuses missing / failed / foreign-dataset PA runs and seed-frame mismatches with field-level issues whose hints name the fix (`test_dpd_rejects_incompatible_pa`); the GUI lists only succeeded PA runs of the dataset and explains when there is none |
| `run_dpd` output is marked as the pre-distorted PA input, never a linearised PA output | done: artifact description, sidecar `dpd-output-meta` (`signal_role: pa_input_predistorted`, statement, column meaning), the `u` stage of the signal chain, `opendpd apply` output text and the Apply dialog all say so (`test_run_dpd_exports_predistorted_input`) |
| The simulation chain records the PA checkpoint hash and the evaluation profile; another surrogate produces a new result without overwriting the old | done: `ModelEvidence.weights_sha256` for PA and DPD, `metric_profile_id/version` in every result; `run_dpd` with `pa_reference` writes its own `result.json` + `results/<profile>.json` (`test_swapping_the_surrogate_yields_a_separate_result` checks the DPD run's result file is byte-identical afterwards) |
| Input scaling, output-power-related metadata and the reference gain are recorded; no dBm / efficiency without physical calibration | done: `EvaluationResult.scaling` (amplitude units, input scaling from the data version, reference gain, `physical_calibration: false`), the validator rejects a calibration claim, the GUI shows the uncalibrated note; nothing derives absolute power |
| Out-of-range amplitude hints have an explainable basis and do not claim to prove surrogate validity | done: `surrogate_coverage` (fitted peak from the surrogate's training split, peak of u, fraction above) with a note that says so in both branches; a non-zero fraction is listed as an extrapolation limitation |
| Baseline and DPD use one consistent, traceable reference; no separate normalisation | done: `baselines` (`surrogate_without_dpd`, `measured_without_dpd`) are scored against the same `reference_gain · x` with the same profile and valid range; the validator requires the same metric set; the GUI table shows them side by side (`ResultDetailPage.test.tsx`) |
| Exported pre-distorted arrays reload with the same order, dtype, amplitude and metadata | done: `test_run_dpd_exports_predistorted_input` rebuilds `u` independently over the whole test split and compares the reloaded CSV (float32, atol 1e-6), the x columns (exact), the row count and the recorded peak |
| Real-browser evidence | done: verified 2026-09-06 in the desktop browser against `opendpd gui` on a scratch workspace holding a CLI-made PA run and DPD run: DPD result page shows the x/u/y chain (y marked simulated), the baselines table (3-epoch smoke DPD: NMSE −20.02 dB vs −11.37 dB surrogate-without-DPD and −10.92 dB measured-without-DPD; ACLR worse than both baselines, shown as is), coverage (0.00 % above the fitted peak) and the uncalibrated note; run page Lineage card lists the PA surrogate; **Apply DPD to the test split…** → new `run_dpd` run succeeded in 3 s, its result reproduces the DPD run's numbers, **Download u** serves the CSV (7680 rows, `I,Q,I_dpd,Q_dpd`) and the sidecar. The pass found and fixed four defects: runs made by the CLI were invisible to the service (now indexed at start-up and on listing, `test_runs_made_by_the_cli_are_visible_to_the_service`), the surrogate select rendered blank for the default, the dialog stayed open after submitting, and a finished run showed "Epoch 4 of 3" |
| Deliverables: DPD workflow, linked experiment graph, signal export, simulation evidence page, integration tests | done: Apply-DPD dialog + `opendpd apply` + `run_dpd_config(pa_run_id=)`; `GET /runs/{id}/lineage` + Lineage card; sidecar export; result page chain / baselines / coverage / scaling; `tests/integration/test_cli_run.py` (15 tests incl. 5 new), API lineage test, schema tests, Vitest pages tests; `docs/protocols/simulation-chain.md` |

## S11 acceptance items

| Item | Status |
|---|---|
| Same-protocol results are comparable; differences in data, operating point, surrogate, metric version or execution semantics are stated explicitly | done: `incompatibilities(a, b)` names profile/version, evidence type, dataset, preprocessing version, split protocol, evaluated split, reference kind, reference gain (operating point), PA surrogate weight hash (simulated DPD results) and execution semantics; `GET /results/compare` and the Compare page show incompatible results side by side with every reason and rank nothing (`test_results_under_different_protocols_are_not_comparable`, `test_plots_history_and_comparison`, `ComparePage.test.tsx`); CSV export carries the same table and reasons |
| Several traces share colours, units, reference and legend; hidden layers never change the backend evaluation | done: one palette entry per result on the table swatch and the spectrum overlay, units from the profile, the reference trace named after the run it comes from; overlays refuse mixed axes/sample rates with a note. Metrics are computed once in the worker and stored; charts are drawn from separate plots-v1 data (`docs/protocols/metric-profiles.md`), so hiding or zooming a trace cannot touch a score. Constellation / EVM plots are **not drawn**: the data carries no demodulation reference, and the plan only allows them when one exists |
| A package carries the resolved configuration, data / model / source identity, environment, seed, metric profile, results and reproduction commands | done: `PackageManifest` (`opendpd/schemas/package.py`): software provenance, run id, task, `config_sha256`, seed, result id + profile, dataset identity (id, raw sha256, data and split version, source kind), references with checkpoint hashes, hashed member list, named commands (`import`, `evaluate`, `rerun`, `legacy`); the run directory travels complete (`docs/protocols/experiment-packages.md`) |
| Full private package vs redacted share package; by default no user PA data or sensitive paths leave the machine | done: `share` excludes worker logs, the event journal and all user data, rewrites machine paths to `<workspace>` / `<home>` / `<host>` and removes the worker identity and the original import path; every redaction is listed in the manifest (`test_share_package_is_redacted_and_says_what_is_missing` greps the whole archive for the workspace path, home and hostname). Packages are files under `<workspace>/exports/`; nothing is uploaded anywhere |
| When the data does not travel, the missing items and how to obtain them are recorded; a checksum only verifies consistency | done: `manifest.missing` + `dataset.how_to_obtain` (dataset id, raw sha256, import command); the import report repeats them and `opendpd evaluate` refuses to score without the data; `test_share_package_re_evaluates_once_the_data_is_imported_with_the_same_hash` and `test_dataset_conflicts_are_refused_not_merged` (a different raw hash under the same id is refused, never merged) |
| Import a full package into a new workspace and re-evaluate the frozen checkpoint within tolerance | done: `test_full_package_round_trips_into_a_new_workspace_and_re_evaluates` (PA run + DPD run with its surrogate reference, new workspace, `evaluate_run` under the stored profile, every metric within rel 1e-4 / abs 1e-5 — tolerance row in `docs/protocols/acceptance-thresholds.md`, **pending maintainer approval**); same through the CLI (`test_export_import_and_report_through_the_cli`) and the API (`test_export_download_import_and_report_routes`) |
| Re-training reproducibility is reported separately; re-computing a stored output is never presented as identical training | done: `manifest.retraining_note` and the Export panel say so; the reports name the re-evaluate and re-run commands as different things; the statistical retraining protocol is S12 |
| Damaged, incomplete, hash-mismatched or unsupported packages are refused with a specific diagnostic | done: `inspect_package` verifies the version, the member list and every hash before anything is written: `not_a_package`, `manifest_missing`, `manifest_invalid`, `unsupported_version`, `unlisted_file`, `missing_file`, `hash_mismatch`, `unsafe_path`; `run_exists`, `reference_conflict`, `dataset_conflict` on import (`test_damaged_packages_are_refused_with_a_specific_diagnostic`, 5 cases); the GUI shows the server reason (`ExperimentsPage.test.tsx`) |
| Reports bound to data and computation sources; the frontend never recomputes metrics | done: `opendpd report` / `GET /results/{id}/report` copy every number from `result.json` and draw the spectrum from stored plots-v1 data (`test_reports_are_bound_to_the_stored_result`); the React pages only render stored values |
| Deliverables: results workbench, exporter/importer, report templates, cross-workspace reproduction test | done: result charts + history + Compare page + Results selection; `opendpd export/import/report`, `POST /exports`, `GET /exports/{id}`, `POST /imports`, Export panel, Import package…; HTML/Markdown reports; `tests/integration/test_packages.py` (11 tests), API route tests, Vitest (40) and Playwright J3 (mock API) |
| Real-browser evidence | done: verified 2026-09-06 in the desktop browser against `opendpd gui` on a scratch workspace with fresh PA / DPD / apply runs on the measured built-in data: the DPD result page draws the spectrum (six named traces: x, u, gain·x, PA(u), the two no-DPD baselines), the time excerpt and AM-AM / AM-PM from stored plots-v1 data; **Export share package** produced a 46 kB zip served as `application/zip` with the redaction list and the retraining note, the HTML report (167 kB) embeds the spectrum PNG and the Markdown report lists the metric table with both baseline columns; **Compare selected (2)** on two PA runs gave the same-protocol verdict, best-per-metric chips and the colour-matched overlay; **Import package…** on a share package made in another workspace (user data, not included) showed the missing-data list with the exact `opendpd evaluate` command, listed the run and opened it with its history curves and artifacts. The pass found three defects, fixed in the closure commit: an imported share run's Artifacts tab still listed the worker log the package had left out (artifact manifests are now restricted to the files that travel, and reference runs carry their results and metric logs), its Logs tab said "No log output yet" for a finished run (now says no log is stored), and `opendpd run --json` mixed the trainer's progress lines into the JSON document (now stderr) |
| G1 gate: import → diagnose → PA → DPD → evaluate → export → CLI re-evaluation on real data | machine side done: `tests/integration/test_g1_flow.py` runs the whole chain through the CLI on the measured built-in data (`datasets import-builtin` → `datasets doctor` → `run --recipe pa-gru-smoke-v1` → `run --recipe dpd-gru-smoke-v1` → `evaluate --profile general-spectral-v1` → `export --kind full` → `import` into a second workspace, built-in data registered and hash-checked → `evaluate` there: every metric and both no-DPD baselines within rel 1e-4 / abs 1e-5 of the packaged result). The same chain was walked in the browser (row above). **Pending human:** at least one internal user not involved in the implementation completes the flow independently — this cannot be produced by the implementation and is recorded as open |

## S12 acceptance items

| Item | Status |
|---|---|
| Every published result has a traceable command, configuration, inputs, source and artifact hashes; report numbers are never hand-edited | done: a benchmark report lists, per entry and seed, the run id, the resolved-configuration hash, the checkpoint hash, the surrogate run id, the selected epoch and the metrics read from the stored result; the dataset audit carries the raw sha256 and versions; software provenance carries the git commit and dirty flag; `report_sha256` seals the content and `load_report` / `check` refuse an edited file (`test_an_edited_report_is_detected`). The commands are those of every run (`config.user.json`, packages, `provenance.json`). The GPU benchmark report keeps its own evidence bundle (`benchmark/collect_benchmark_report.py`); it is reused, not duplicated |
| Fixed-checkpoint re-evaluation is a deterministic regression; re-training is a statistical regression; different thresholds | done: `docs/protocols/acceptance-thresholds.md` separates the deterministic layer (goldens, checkpoint re-evaluation, package round trip, least-squares fits) from the statistical layer (`benchmark-v1` bands: mean ± max(0.5 dB, 2 × sample std) over ≥ 3 seeds, per pre-registered plan); `RegressionBaseline` applies to plan reports only |
| Formal comparisons of stochastic models use at least 3 pre-registered seeds, report every seed and the aggregate, and do not claim generality | done: `BenchmarkPlan.seeds` requires ≥ 3 distinct seeds fixed before execution (`test_a_plan_needs_three_pre_registered_seeds_and_is_frozen_by_its_hash`); `SeedScore` rows stand next to `MetricStats`; every report carries the note that the spread of N seeds is not evidence of generality (`test_report_binds_every_number_to_runs_and_hashes_and_reports_each_seed`) |
| Equal parameter count is not presented as equal compute cost; ILA/DLA and other training-path differences are shown | done: every report carries the compute-cost note and per entry the parameter count, family-independent training path, look-ahead and execution semantics; every result names its training path (`gradient`, `gradient_dla`, `least_squares`, `ila_least_squares`) on the result page, in reports and in the comparison (`test_compare_states_the_training_path_but_ranks_under_one_protocol`) |
| PA surrogate training, DPD optimisation and final test data relationships are auditable; surrogate cross-validation is not called physical validation | done: `DataAudit` in every report (surrogate trained on train; DPD optimised on train through the frozen surrogate of the same seed or, for ILA, on measured data; selection on val; test reported once) with the statement that this is not physical validation; per run the lineage (S10) and the x/u/y chain make the same relationships explicit |
| Baseline fitting stability and tuning budget are recorded; no gains from obviously broken classical implementations | done: rank, condition number, cutoff (`rcond`) and train residual are stored in `fit.json`, repeated in the result's limitations and in the benchmark report; the cutoff is fixed by the recipe (protocol), not tuned per result; `test_gmp_fit_with_a_cutoff_reports_the_retained_rank_and_its_lookahead`, `test_rank_deficiency_is_reported_and_the_cutoff_truncates`; the MP fit of the synthetic memory-polynomial PA reaches the float32 noise floor (`test_mp_fit_identifies_the_synthetic_pa_and_records_the_fit`) |
| Leaving the preset regression range blocks automatic release; an accepted degradation carries a reason and a human approval | done in code: `opendpd benchmark check` exits 1 when any metric of an approved baseline leaves its band in either direction; an unapproved baseline is advisory; a baseline binds to one plan hash; accepting a degradation means a new baseline file with `notes`, `approved_by`, `approved_on` under the protected path `benchmark/regression/` (`test_baselines_block_only_when_approved_and_only_for_their_plan`, CLI test). **Pending human:** no baseline is approved yet; `benchmark/regression/cpu-regression-dpa-200mhz/baseline.json` is a draft from this machine (see `report.md` there) awaiting a maintainer's review |
| Public leaderboard submissions are not executed on machines connected to lab data, credentials or instrument networks | policy recorded in `docs/protocols/benchmark-protocol.md`; **pending human**: an organisational rule the software cannot verify |
| Deliverables: versioned benchmark protocol, regression baselines, CPU/GPU verification flows, machine-readable reports | done: `benchmark-v1` (schema + protocol doc), `cpu_regression` and `gpu_full` tiers with fixed matrices, weekly CPU run + advisory check (`.github/workflows/weekly.yml`), JSON + Markdown reports; GPU full validation is a human-triggered run of the `gpu_full` plan on a named machine (not executed here: no GPU evidence in this environment) |

## S13 acceptance items

| Item | Status |
|---|---|
| G2 performance targets of plan §8.2 reach the frozen thresholds; every report records machine, data and software versions | done with two honest corrections, all numbers in `docs/releases/performance-report.md` (generated by `scripts/perf_report.py`, machine/data/software recorded): startup p95 0.39 s (≤ 10 s), ready → experiments table p95 168 ms (≤ 2 s), tab switch p95 108 ms (≤ 200 ms), status API p95 19.8 ms while training (≤ 250 ms), chart re-render p95 196 ms (≤ 300 ms), event latency p95 0.31 s / max 0.36 s over 245 events (≤ 2 s), GUI overhead on training median 1.7 % over 3 alternating pairs (≤ 5 %), control-plane growth 0.2 MB over 30 min / 16 281 requests / 90 runs (≤ 100 MB), stress import of 10⁸ paired samples (1.6 GB) extra anonymous memory 2.9 MB, memory-mapped file pages 2.3 GB reported separately (≤ 1 GB), cancel visible after 3.6 ms (≤ 1 s), 50 000-line log windowed to 26 DOM rows and 1 000 runs paged to 50 rows, offline journey. The first full run reported GUI overhead 12.8 % and latency max 2.4 s: both were measurement bookkeeping (supervisor timestamps; events emitted before the poll loop opened), fixed in the runtime and the harness and documented in `docs/releases/hardening-report.md` "Performance"; no target was relaxed |
| Packaged GUI passes install, training smoke, export and exit on the support matrix (not only `npm run dev`) | done on Linux: `tests/packaging/test_wheel_install.py::test_wheel_ships_gui_and_one_command_serves_it` installs the wheel in a fresh venv and drives the packaged service over HTTP (bootstrap → dataset → 2-epoch run → result → share package → clean exit); **pending human** on macOS and Windows desktops (`docs/releases/support-matrix.md`) |
| Chromium, Firefox and WebKit cover the main journeys; real Safari and default-browser launch verified separately | done for Chromium (1366×768, 1920×1080) and Firefox locally and in CI, WebKit in CI (`ci.yml`, `weekly.yml`; this host cannot launch the Linux WebKit build); **pending human**: real Safari and the system default browser |
| Long logs, large data, repeated page switching and operations during training cause no unbounded memory or event-queue growth | done: 30-minute load in the performance report (control-plane growth within the target), windowed log viewer (`LogViewer`, 2 000-line pages, bounded DOM), server-side paging and search of runs, memory-mapped and chunked NumPy import, bounded doctor window (`test_studio_api.py::test_logs_are_paged`, `test_runs_are_paged_and_searchable_on_the_server`, `test_datasets_service.py::test_numpy_imports_stream_from_the_source_without_copies`, `test_doctor_analyses_a_bounded_central_window_and_says_so`) |
| Import, preview and model loading execute no arbitrary pickle or script; the trusted legacy-checkpoint path is labelled and tested separately | done: one restricted loader (`legacy_adapter.load_checkpoint`, `weights_only=True`) used by evaluation and the baselines; a `__reduce__` payload is refused and its side effect never happens (`test_hardening.py`); legacy `state_dict` checkpoints still load (`tests/golden/test_legacy_checkpoint_loads.py`); no `torch.load` without `weights_only`, no `allow_pickle`, no `pickle.load` in the package |
| Negative tests for malicious Origin, cross-site writes, path traversal, symlink bypass, XSS, zip extraction bounds and oversized requests | done: `tests/integration/test_hardening.py` (16 tests) plus the S04/S07/S11 tests, one row per threat in `docs/architecture/threat-model.md`; summarised in `docs/releases/hardening-report.md` |
| Dependency and security scans and manual review leave no unhandled release blocker; accepted residual risks are recorded | done: `pip-audit` and `npm audit` clean after upgrading pillow and setuptools in the venv (2026-09-06), flake8 critical selection 0, boundary reviewed; residual risks listed in the threat model |
| Automated accessibility checks and a manual keyboard path pass; scientific results never rely on colour alone | done: axe-core WCAG 2.1 A/AA on every page and every tab of a live run (`frontend/e2e/a11y.spec.ts`), keyboard-only journey, dash pattern and marker symbol per series, labelled status chips; **pending human**: a screen-reader pass |

Deliverables: `docs/releases/performance-report.md` (generated by
`scripts/perf_report.py`), platform evidence and security checks in
`docs/releases/hardening-report.md` (with the migration and recovery table),
release candidate `2.2.0.dev0` built with `python -m build` from `68953a1`:

```
83bbb3cc1bcc99b6bbc6de800f1561354bbb28c33f007beb104317bca1ef9196  opendpd-2.2.0.dev0-py3-none-any.whl
eb934031bf2a36361b45d6913a56a6128df39239cd355f558ed6733b6f3d92ba  opendpd-2.2.0.dev0.tar.gz
```

The candidate must be rebuilt from the commit a maintainer tags; these
checksums identify the S13 build only.

## S14 acceptance items

| Item | Status |
|---|---|
| Three external groups each complete at least one real task; missing attempts and failures count | **pending human**: the trial has not happened; the measurement protocol is fixed in `docs/releases/external-trial-protocol.md` so the report will be comparable and honest |
| Onboarding test with ≥ 5 target users, ≥ 4 complete the example flow without a maintainer changing code; operation time and training wait recorded separately | **pending human**: same protocol (two clocks per participant, hints recorded verbatim) |
| Blocking problems in install, data import, first training and result interpretation have verified fixes or explicit degradation paths | done for the problems found internally (S11 browser defects fixed with tests; degradation paths in the quickstart's "When something is wrong" and the doctor's suggestions); external blockers are collected by the trial |
| The main documented commands run in CI; a GUI-exported configuration runs through the CLI | done: `tests/integration/test_docs_commands.py` executes every `opendpd …` command family shown in the tutorials in a fresh workspace (benchmark commands run in `weekly.yml` and the protocol tests) and checks that every documented flag exists in the parser; the same test takes a run's resolved configuration (what "Download configuration" serves), drops the `resolution` block and runs it through `opendpd validate` and `opendpd run` to the same configuration hash; `test_entry_consistency.py` proves GUI and CLI agree on the numbers |
| Wheel/sdist, checksums, release notes and support matrix published; demo material from the same candidate | candidate built (checksums in the S13 entry), `docs/releases/release-notes-2.2.0.md`, `docs/releases/support-matrix.md`; **pending human**: publication, tag and demo recordings |
| Data sharing, problem reports and anonymous diagnostic packages happen only by explicit user action; no telemetry or experiment data is sent by default | done: nothing in the service or the page contacts any host but the loopback one (`test_the_service_and_the_page_never_call_out`, CSP `connect-src 'self'`, offline journey); the share package is the diagnostic package: produced only by an explicit export, with user data, worker logs and machine paths removed and listed under `redaction` |
| Maintainer release approval; the agent never pushes a formal release, data or models to external platforms | **pending human**; rule recorded in `AGENTS.md` §6 and `docs/releases/release-notes-2.2.0.md` |
| Deliverables: first public Studio version, external trial report, tutorials and contribution guide, prioritised backlog | candidate and tutorials done (`gui-quickstart`, `headless-cli`, `adding-a-model`, `CONTRIBUTING.md`); backlog in `docs/releases/backlog.md`; public version and trial report **pending human** |

## S15 acceptance items

| Item | Status |
|---|---|
| Every profile states its standard version/clause references, its support and its non-support; no generic "3GPP compliant" claim | done in scope, **pending human** for the version: `docs/protocols/waveform-profiles.md` §1–2 name the numerology (TS 36.211 §6.12), the EVM principles (TS 36.104 Annex E) and the ACLR definition (TS 36.104 §6.6.2) with "version fixed at cross-validation", list what the waveform is not, and the profile description says "not a conformance measurement" (`test_profile_is_registered_but_pending_cross_validation`) |
| Users without MATLAB can run the published reference vectors and the evaluation; MATLAB is only the cross-validation backend | done: `opendpd waveforms generate/show`, `datasets import --waveform`, `opendpd run` and `opendpd evaluate --profile ofdm-lte20-evm-v1` run in Python end to end (`test_documented_commands_run_end_to_end` on a synthetic capture); the cross-validation procedure (§6 of the protocol) is a maintainer step |
| Tests for the ideal signal, known noise, distortion, timing/frequency perturbations and misconfiguration | done: `tests/unit/test_waveform_ofdm.py` (ideal EVM < 1e-9 %, AWGN of known SNR within 0.03 pp of the closed form, cubic distortion monotonic, timing offsets and looped playback, ±350 Hz offset removed, 4-tap channel equalised exactly, rate-conversion floor 0.037 %, refused signals) and `tests/unit/test_metrics_ofdm_evm.py` (unbound, unsupported rate, missing metadata, non-finite, non-correlating inputs are explicit statuses) |
| Initial comparison targets on the same digital reference: adjacent-band ratio within 0.1 dB, RMS EVM within 0.05 pp; floors and reference uncertainty recorded | targets and floors recorded in the protocol (§4–5); the comparison itself is **pending human** (§7 "not run yet") |
| The profile's error budget is approved before model comparisons start; never relaxed afterwards | budget proposed in §5; **pending maintainer approval**; the profile stays `pending_cross_validation` (hidden in the GUI, limitation on every result) until then |
| Any I/Q input that does not match the profile returns "not applicable / missing reference" and is never re-scored under another definition with the standard's name | done: unbound datasets, non-correlating signals, unsupported rates and the built-in ten-carrier captures get explicit statuses (`test_unbound_dataset_is_missing_reference_never_scored`, `test_unsupported_capture_rates_and_missing_metadata_are_explicit`, `test_a_bound_dataset_whose_signal_is_not_the_waveform_is_missing_reference`) |
| Historical metrics and old-paper reproductions are unaffected | done: `legacy-opendpd-v1` stays frozen and golden-pinned (`tests/golden`, `test_legacy_profile_through_the_registry_reproduces_the_frozen_goldens`); the new profile is an additional stored file per run |

Deliverables: waveform + profile (pending cross-validation), reference-vector
packages, GUI gating by validation status (`frontend/src/api/profiles.ts`),
`docs/protocols/waveform-profiles.md` with the validation record to be
filled by the maintainer. Protected paths touched (`opendpd/core/metrics/**`,
`docs/protocols/**`): this stage's commit is the separate science-review
change AGENTS.md §3 requires and is labelled as such in its message.

### G2 gate

Not passed. End-to-end, trustworthy numbers, cross-platform install (Linux),
security and performance hold with evidence; external usability (S14 trial
and onboarding) and the macOS/Windows/Safari desktop checks are pending
people. Per the plan, G2 is not marked complete on internal evidence alone.
