# OpenDPD 下一阶段开发计划
## 可信实验基础设施与 OpenDPD Studio

**文档版本：** 1.0  
**编写日期：** 2026-09-06  
**面向对象：** OpenDPD 维护者、研究人员、前后端开发者与 coding agents  
**已确定的 GUI 技术路线：** FastAPI + React + TypeScript  
**主要交付形态：** 本地优先的浏览器实验工作台，与 Python API、CLI 共用计算核心  
**仓库基线：** `lab-emi/OpenDPD`，commit `7426bbf8a47624b59bd7f045a86641b403023f3c`。[R1]

> 本文是一份待执行的开发与验收计划，不代表其中的功能已经实现或测试通过。除明确标为“现状”的部分外，命令、目录、接口、性能数字及流程均为设计目标。性能和可用性指标是初始验收要求，不是当前项目的实测成绩。

---

## 1. 产品目标与开发原则

### 1.1 核心目标

让一个不熟悉 OpenDPD 内部代码的外部用户，能够通过浏览器完成：

**导入自己的 PA 数据 → 检查数据质量 → 训练 PA 模型 → 训练和运行 DPD → 理解评估结果 → 导出可复现实验。**

同时，让研究者可以从 GUI 导出同一个实验的配置，通过 CLI 或 Python API 执行，并把方法和结果贡献回社区。

项目的主要成功指标是**外部用户独立完成可信实验的数量与比例**，而不是 GUI 页面数量、agent 提交数或新增网络数量。

### 1.2 必须坚持的原则

1. **一个计算核心，三个入口。** GUI、CLI 和 Python API 不得分别实现训练、预处理或指标算法。
2. **GUI 与核心能力同步交付。** 每项面向用户的新能力同时包含接口、界面、无界面运行方式、文档和测试。
3. **先完成端到端闭环，再扩展功能。** 不把完整 NR/LTE 工具箱、公共排行榜或仪器全覆盖作为首版前置条件。
4. **默认本地、默认私有。** 不要求账户、云服务或数据上传；默认不启用遥测。
5. **科学语义不可静默改变。** 指标定义、数据划分、模型上下文、预处理和证据类型必须版本化。
6. **GUI 不承担训练进程。** 页面刷新、浏览器断开与训练生命周期分离；训练不在 FastAPI 请求回调中直接执行。
7. **兼容现有工作。** 不通过一次性大重写破坏既有脚本、Python API、checkpoint 和论文复现路径。
8. **agent 执行已批准任务，人决定科学规则和发布。** 不允许 agent 为通过测试而自行放宽门槛或改变 benchmark 协议。

### 1.3 首版范围与明确不做的事情

首个公开版本包括：单用户本地工作区、数据检查、参考 recipe、PA/DPD 训练、任务管理、结果比较、实验导出，以及跨平台一键启动。

首版不包含：多租户云平台、任意 Python 在线执行、拖拽式神经网络搭建器、通用工作流编排器、所有仪器驱动、自动硬件闭环、所有模型的断点恢复、全 NR/LTE 标准覆盖，以及多机分布式训练。

这些项目不是永久排除，而是必须由真实需求和独立验收关卡触发，不能“顺手”加入首版。

---

## 2. 已核实的现状与迁移约束

| 当前事实 | 对本计划的约束 |
|---|---|
| 基线 `pyproject.toml` 声明版本 `2.1.0`，命令入口是 `opendpd-cli`，尚未声明 `gui` extra。[R2] | 新增 `opendpd gui`，保留旧命令；不能把新命令写成当前已有功能。 |
| `opendpd/api.py` 中的训练函数通过修改全局 `sys.argv` 构造参数，再同步执行训练。[R3] | 在接入并发请求前建立显式配置与独立任务进程；不能把现有 API 直接放入多个 GUI 线程并行调用。 |
| 现有 benchmark 已记录配置、来源和局限，并明确区分 surrogate 仿真与真实 PA 测量。[R4] | 复用并扩展现有证据链，不另造一套互不兼容的 benchmark 系统。 |
| 当前 benchmark 记录了历史指标口径、分段执行、lookahead、单种子等限制。[R4] | 历史指标保留版本；新评估不混入旧结果；流式执行和多种子评估独立验收。 |

本文没有重新运行完整训练或 GPU benchmark。S00 必须建立真实运行基线，并记录任何当前无法复现的部分。

---

## 3. 用户启动体验：必须实现的产品契约

### 3.1 普通用户的目标操作

首次安装到适当的 Python 环境：

```bash
pip install "opendpd[gui]"
```

日常启动只需要一条命令：

```bash
opendpd gui
```

成功行为必须是：**启动本地服务 → 确认服务和前端资源就绪 → 自动打开系统默认浏览器 → 显示工作台。**

> **修订（2026-09-11，维护者决定）**：安装 `opendpd[desktop]` 后，`opendpd gui` 默认在原生应用窗口（pywebview）中打开工作台；未安装该 extra、无桌面会话或使用 `--browser` 时仍按上文打开默认浏览器。`--window` 强制窗口，`--no-browser` 行为不变。详见 ADR-0002。

用户不需要安装 Node.js、运行 `npm`、启动 Vite、复制地址到浏览器，或打开第二个终端。Node.js 仅用于前端开发和发布构建；Vite 生成的静态资源随 Python 分发包发布，由 FastAPI 提供。[R5][R6]

保留以下高级入口：

```bash
opendpd gui --workspace "./my-workspace"
opendpd gui --no-browser
opendpd gui --port 8765
opendpd doctor
opendpd run --config "./experiment.json" --workspace "./my-workspace"
```

`--port` 未指定时自动选择可用端口；显式指定而被占用时返回可操作的错误，不偷偷换端口。浏览器自动打开使用 Python 的跨平台浏览器控制接口，并处理启动失败和无图形环境。[R7]

### 3.2 生命周期与异常行为

| 场景 | 必须发生的行为 |
|---|---|
| 正常启动 | 页面只在服务就绪后打开；默认绑定 `127.0.0.1`；控制台显示状态和本地访问地址。 |
| 浏览器启动失败 | 服务保持可访问，提示手动打开地址；不能因此终止已启动的任务。 |
| 无桌面环境或 SSH 会话 | `--no-browser` 正常工作；不启动会阻塞的文本浏览器；解释如何安全访问服务。 |
| 用户关闭标签页或浏览器 | 只要服务仍运行，任务继续；重新打开后恢复显示已有任务。 |
| 用户按 Ctrl-C 关闭服务 | 按已定义的退出策略停止接收任务，通知正在运行的任务，执行有界等待和进程树清理；不留下未标记的训练进程。 |
| 服务崩溃或主机重启 | 下次启动将未完成任务标为 `interrupted` 并显示原因；不声称训练已自动恢复。 |
| 同一工作区重复启动 | 验证实例身份和版本后复用已有服务并打开页面，或明确拒绝；不得同时启动两个工作区写入者。 |
| 工作区只读、磁盘不足 | 启动或任务预检查失败，并指出处理方法；不能先训练到最后才发现无法保存。 |

**“浏览器重连”“服务重启”“从 checkpoint 恢复训练”是三个不同功能，必须分别实现和验收。**首版支持前两者的正确状态处理；只对明确支持的模型/训练器开放 checkpoint resume。

### 3.3 支持范围

首版目标测试平台为 Windows x86-64、macOS Apple Silicon、Linux x86-64；具体操作系统版本和 Python 版本在 S00 固定到支持矩阵。GUI 优先验证 Python 3.11–3.13，不因此静默取消已有核心 API 的兼容承诺。

分别公布三张能力表：**GUI/CPU 支持、CUDA 支持、MPS 支持**。某个平台能打开页面不意味着所有模型和加速路径都受支持。没有真实测试证据的组合标为“未验证”，不得标为“全部跨平台”。

---

## 4. 总体技术架构

### 4.1 技术选型与边界

| 层级 | 确定的技术/方案 | 职责和限制 |
|---|---|---|
| 计算核心 | 现有 Python/PyTorch/NumPy/SciPy 能力逐步模块化 | 训练、推理、数据处理、指标；不依赖 FastAPI 或前端组件。 |
| 配置与数据契约 | 版本化模型；建议使用 Pydantic v2 统一校验 | 配置可独立于 GUI 使用；如作为核心依赖加入，必须声明并测试其兼容性。 |
| 应用服务 | Python 服务层 | 数据集、实验、recipe、结果和任务操作的唯一业务入口。 |
| 任务执行 | 本地单 supervisor + 独立 worker 进程 | 每个训练任务使用独立进程和输出目录；首版同一设备默认串行排队。 |
| 持久化 | 工作区目录 + SQLite 元数据 + 文件产物 | 大型数组和 checkpoint 不进入数据库；首版工作区位于本机磁盘，不承诺网络共享盘并发写入。 |
| Web API | FastAPI + Uvicorn | `/api/v1`；请求校验、权限、任务提交、结果和静态前端。 |
| 状态推送 | REST + SSE，必要时降级为轮询 | 单向进度优先 SSE；训练控制使用 REST；首版不同时维护两套推送协议。 |
| GUI | React + TypeScript + Vite | TypeScript strict；页面、导航、表单和可视化。 |
| 组件体系 | 建议统一采用 MUI；主题与领域组件自行封装 | 不混用多套组件库；科学图表可使用独立绘图库。 |
| 科学图表 | 建议使用 Plotly.js 的受限、按需构建版本 | 以 S05/S13 的真实数据测试决定配置；不直接向浏览器传送完整大数组。 |
| 前端测试 | Vitest + React Testing Library + Playwright | 单元、交互、视觉回归和跨浏览器端到端测试。 |
| 后端测试 | pytest + API/进程/数值参考测试 | 测试必须包含失败路径，不以覆盖率替代正确性。 |
| 分发 | Python wheel/sdist 携带预构建前端资源 | 最终用户无 Node.js、Redis、Docker 等额外服务前置条件。 |

> **修订（2026-09-11，维护者决定）**：界面提供英、法、德、西、中、日、韩七种语言，顶部栏提供带国旗和语言名称的切换菜单，选择按工作区保存（`settings.json`）；服务端文本、日志、报告与 CLI 保持英文。详见 ADR-0003。

组件库、图表库的建议在 S01 通过一份 ADR 固化；一旦通过，不允许 agent 在普通功能 PR 中更换技术栈。各依赖的确切版本由 S00/S01 验证后锁定，不在计划中臆测“最新版本号”。

FastAPI 官方将重计算与轻量后台任务区分开来；本项目明确选择独立进程执行训练，而不是把训练直接交给 `BackgroundTasks`。[R8]

### 4.2 模块边界

以下为目标组织方式，可逐步迁移，不要求一次性搬完现有文件：

```text
opendpd/
  core/                 # 数据、指标、训练接口、模型能力描述
  schemas/              # 配置、数据检查、任务与结果的版本化契约
  services/             # 可被 GUI、CLI、Python API 共用的应用服务
  runtime/              # 工作区、任务 supervisor、worker、事件与产物管理
  server/               # FastAPI app、路由、本地会话与静态资源服务
  studio/static/        # 发布流程生成的 React 构建产物
  cli.py                # 新入口与旧入口兼容适配
frontend/
  src/app/
  src/features/         # datasets、experiments、results、settings
  src/components/       # 公共组件与 RF 领域组件
  src/api/              # 从 OpenAPI 生成的类型/客户端及薄封装
  src/test/
tests/
  unit/
  integration/
  golden/
  packaging/
  e2e/
docs/
  architecture/
  protocols/
  tutorials/
  releases/
```

前端只负责显示与交互，禁止自行计算正式 EVM/ACLR。OpenAPI 和 schema 是前后端契约来源；schema 变化必须触发类型生成及契约测试，避免手写接口长期漂移。

### 4.3 工作区与实验产物

```text
workspace/
  workspace.json
  metadata.sqlite
  datasets/<dataset_id>/
    manifest.json
    raw/                 # 保留原始数据；对已注册版本不可覆盖
    processed/<version>/
    diagnostics/
  runs/<run_id>/
    config.resolved.json
    provenance.json
    events.jsonl
    logs/
    checkpoints/
    results/
    artifacts.json
  cache/                 # 可重建的可视化/预处理缓存
  exports/
```

所有任务使用不冲突的 `run_id`。路径、模型、数据、指标版本和依赖产物必须显式关联。写文件采用临时文件加原子替换等可恢复策略；“完成”状态只能在必需产物验证成功后提交。

数据库 schema 与实验 schema 分别版本化。迁移前备份，迁移失败不能破坏旧工作区；旧结果的缺失元数据标为未知，不编造来源。

### 4.4 首版任务状态机

```text
queued -> running -> succeeded
                  -> failed
                  -> cancel_requested -> cancelled
                  -> interrupted
queued -> cancelled
```

`cancel_requested` 不是 `cancelled`。只有确认 worker 退出或到达约定安全停止点后才能显示取消完成。任务重试生成新的 run，并记录父 run；不得覆盖失败记录。已经取消的任务不能因迟到的完成事件重新变成成功。

---

## 5. 开发总览与并行关系

### 5.1 阶段与交付关卡

| 阶段 | 包含步骤 | 阶段目标 | 放行条件 |
|---|---|---|---|
| G0：可安装的 GUI 基座 | S00–S06 | 一条命令自动打开真实打包后的浏览器工作台 | 安装、启动、会话安全、工作区和最小任务链通过；样例/模拟数据明确标注。 |
| G1：可信实验闭环 | S07–S11 | 自有数据到 PA/DPD 训练、比较和复现实验包 | 至少一条真实数据端到端流程通过 GUI/CLI 一致性测试。 |
| G2：可对外使用的版本 | S12–S14 | 回归可靠、性能达标、外部用户能独立完成任务 | 数值、跨平台、权限、性能及试用关卡全部通过。 |
| G3：科学与部署扩展 | S15–S19 | 限定标准评估、实测、多工况、流式和定点能力 | 每项独立验收并独立发布，不要求全部做完才发布已有成果。 |
| G4：社区基准 | S20 | 外部结果可提交、复核和公平展示 | 协议冻结、独立复现和治理成立；不同证据类型分榜。 |

正式版本号由维护者依据兼容性决定；本计划不以“大版本号”代替验收。

### 5.2 每一步的依赖

| 步骤 | 内容 | 主要依赖 | 可并行部分 |
|---|---|---|---|
| S00 | 基线、范围、治理与测试底座 | 无 | 文档和构建审计 |
| S01 | 用户旅程、设计系统与接口契约 | S00 | 前后端共同设计 |
| S02 | 显式配置、注册表、工作区 | S00–S01 | 与 S05 并行 |
| S03 | 任务执行与生命周期 | S02 | 与 S04/S05 并行 |
| S04 | FastAPI、事件与本地安全 | S01–S02 | 路由先用 mock，任务连接依赖 S03 |
| S05 | React GUI 基座 | S01 | 使用契约 mock，不等待训练改造完成 |
| S06 | 打包与一键启动 | S04 基础服务、S05 | 早做安装验证，不等功能全部完成 |
| S07 | 数据导入、诊断和预处理 | S02、S04、S05 | 与 S08 并行 |
| S08 | 指标版本与评估证据 | S02、S04、S05 | 与 S07 并行 |
| S09 | PA 训练与实验管理 | S03、S07、S08 | 页面先对接 mock |
| S10 | DPD 训练、推理与 surrogate 评估 | S09 | 结果页与 S11 并行 |
| S11 | 结果比较与实验导出 | S07–S10 | 导出 schema 可提前设计 |
| S12 | 可信 benchmark 与回归 | S08–S11 | 复用原 benchmark，不另起系统 |
| S13 | 性能、安全、无障碍与平台加固 | 从 S03–S06 开始；最终依赖 S11–S12 | 是持续工作，不是最后才开始测试 |
| S14 | 外部试用、文档与公开发布 | S06、S11–S13 | 教程随功能同步编写 |
| S15 | 限定波形及标准评估 profile | S08、S11 | 不阻塞 G2；先做一个配置 |
| S16 | 真实 PA 测量与仪器适配 | S07、S10、S11、S13 | 手工采集结果导入先于自动仪器控制 |
| S17 | 多工况与适应性基准 | S12、S16 的证据 schema | 可先使用人工采集的数据 |
| S18 | 流式执行与因果/延迟语义 | S02、S08、S10、S12 | 与 S15–S17 并行 |
| S19 | 位精确参考与部署导出 | S18 | 先一个模型、一个精度方案 |
| S20 | 社区贡献与版本化榜单 | S12、S14；扩展赛道依赖对应步骤 | 不要求 S15–S19 全部完成才发布基础赛道 |

**并行开发规则：** GUI 可以先使用 mock 开发，但 G1 之后的功能完成证明必须来自真实 API 和真实计算。mock 成功不能被计作端到端成功。

---

## 6. 逐步开发与验收

### S00｜建立基线、开发边界与 agent 规则

**目标：** 在改动前知道哪些行为需要保留、哪些问题真实存在、什么证据可以证明没有回归。

**开发内容：** 固定基线 commit；在干净环境运行现有核心测试和一个小型真实训练流程；记录完整 benchmark 的运行条件及未验证项。建立 `AGENTS.md`、`CONTRIBUTING.md`、PR 模板、CODEOWNERS 和 ADR 模板。建立 Python、前端、打包和数值测试的分层 CI；创建 tiny/standard/stress 数据夹具与性能测试协议。

**验收标准：**
- [ ] 保存 `baseline-report.md`，逐项给出命令、环境、结果和失败原因；不把缺少 GPU 写成通过。
- [ ] 现有 CLI/API 的兼容用例、历史指标 golden fixtures 和至少一个旧 checkpoint 的加载用例已登记。
- [ ] 维护者批准 G0–G2 范围、支持矩阵、风险目录和验收阈值。
- [ ] 普通 PR 不能直接改写数值参考结果、数据划分或验收门槛；这些路径有人工审核规则。
- [ ] agent 的可写范围、资源预算、最大待审 PR 数和停止条件已配置。

**交付物：** 基线报告、测试数据 manifest、CI 骨架、治理文件、首批带验收条件的 issues。

### S01｜确定用户旅程、设计系统与数据契约

**目标：** 前后端围绕同一套实验概念并行开发，而不是各自定义一套状态和字段。

**开发内容：** 明确“复现示例”“使用自己的数据”“添加新方法”三条旅程；确定导航与页面信息层级。固定配色、字体、间距、表单、状态标签、图表与空状态规范。定义 `DatasetManifest`、`DiagnosticReport`、`ExperimentConfig`、`RunRecord`、`MetricProfile`、`EvaluationResult`、`ArtifactManifest` 和 `RunEvent`。前端默认英文，便于国际社区使用；文案集中管理，为后续中文界面留接口。

**验收标准：**
- [ ] 三条用户旅程各有成功、失败和退出路径，不依赖聊天说明才能理解。
- [ ] 首页、数据、实验、结果、设置页面结构通过维护者审阅；高级参数默认折叠。
- [ ] 每个结果必须能表达 `pa_modeling`、`dpd_surrogate` 或 `dpd_measured`，并包含来源和指标版本。
- [ ] schema 示例包括缺少元数据、指标不适用、任务中断、日志断线和旧数据导入。
- [ ] 一个前端原型可用固定 mock 展示完整旅程；mock 明确标注，不能作为科学结果导出。

**交付物：** UX 规格、设计 tokens、组件目录、schema 初版、mock fixtures、技术选型 ADR。

### S02｜消除全局参数耦合，建立统一核心入口

**目标：** 同一个实验配置可通过 GUI、CLI、Python API 执行，且不互相污染。

**开发内容：** 让配置显式传入应用服务和训练器；逐步移除新路径对 `sys.argv`、当前目录和共享全局对象的依赖。保留旧入口适配层。建立模型注册表和能力描述，包括模型参数、训练方法、可用设备、状态、lookahead 和导出能力。数据和结果写入用户工作区，不写入安装包目录。拆除启动 GUI 时不必要的重量级 eager imports。

**验收标准：**
- [ ] 两个不同实验先后执行，第二次配置不受第一次影响；并行 worker 也不共用输出文件。
- [ ] GUI、CLI、Python API 的配置归一化结果一致；默认值有唯一来源。
- [ ] 模型列表来自注册表，CLI choices 与 GUI 选项不重复手写；不存在的模型无法提交。
- [ ] MP/GMP 等非神经方法可表达自身拟合/ILA 逻辑，不被迫套进神经网络训练接口。
- [ ] 旧命令与公开 API 的兼容测试通过；弃用项给出明确迁移提示。
- [ ] 普通 `pip install opendpd` 无需 GUI 依赖；只读安装目录下仍能在外部工作区运行。

**交付物：** 显式配置入口、兼容适配器、注册表、工作区服务、相关回归测试。

### S03｜建立独立任务进程、持久化事件与失败恢复

**目标：** 长时间训练不会阻塞 UI，也不会因标签页关闭而丢失。

**开发内容：** 实现单 supervisor、设备队列、独立 worker、事件序号、心跳、日志和 checkpoint 产物登记。初期可在 worker 内调用兼容适配器，但不得在线程中并发修改全局参数。任务提交支持幂等键；实现合作式取消、有界强制终止和进程树清理。限制并发、日志缓存、磁盘使用和事件传输速率。

**验收标准：**
- [ ] 同一幂等键重复提交不创建重复任务；不同任务拥有独立 run 目录。
- [ ] 一个任务训练时，API 仍能查询状态、提交排队任务和处理取消。
- [ ] 关闭并重开浏览器后仍能看到同一个 run；事件可从上次序号继续获取。
- [ ] 强杀 worker、OOM、磁盘写入失败和配置错误均产生明确终态与诊断信息。
- [ ] 服务异常退出后重启，未完成 run 被识别为 `interrupted`；不产生幽灵“运行中”任务。
- [ ] Windows/macOS/Linux 的取消与退出测试不残留子进程；不根据 PID 单独判断进程身份。
- [ ] 首版没有实现的 pause/resume 按钮不显示；支持 resume 时单独记录 optimizer、scheduler、随机状态及恢复限制。

**交付物：** 任务运行时、状态机测试、故障注入测试、事件格式与操作说明。

### S04｜实现 FastAPI 服务、契约接口与本地安全边界

**目标：** 为前端提供稳定、可验证、默认安全的业务接口。

**开发内容：** 实现健康状态、设备能力、模型、数据、实验、任务、事件和产物 API；统一错误结构。通过 SSE 推送可重放事件，REST 处理控制操作。实现同源访问、本地会话、Host/Origin 校验、写操作防跨站保护、文件访问边界和请求限制。核心操作通过应用服务执行。

**验收标准：**
- [ ] OpenAPI 可生成可用 TypeScript 类型；CI 检测 schema 与已提交类型是否同步。
- [ ] 非法配置在启动 worker 前返回结构化错误，包括字段、原因和下一步建议。
- [ ] 未授权会话不能读取实验数据、下载产物或提交训练；恶意网页不能直接操控本地服务。
- [ ] 路径穿越、符号链接越界、超大上传、非法压缩包和任意 Python 执行入口被拒绝。
- [ ] API 不直接接受并执行 shell 字符串；worker 参数使用结构化、经过验证的输入。
- [ ] 事件断线重连无重复业务动作；历史游标失效时明确返回快照重同步要求。
- [ ] 静态前端路由与 `/api/v1` 不互相覆盖，未知 API 返回 API 错误而不是 `index.html`。

**交付物：** API、生成客户端、权限和契约测试、本地威胁模型。

### S05｜实现 React 工作台与领域组件

**目标：** 得到可长期维护、易理解的界面，而不是临时 dashboard。

**开发内容：** 实现固定导航、工作区选择、实验状态、数据/实验/结果页面框架，以及统一表单。封装 `MetricCard`、`EvidenceBadge`、`SpectrumPlot`、`IQPreview`、`DiagnosticItem`、`RunTimeline`、`ConfigDiff` 等领域组件。加入 loading、empty、error、disconnected 状态。前端状态分为服务器状态、URL 导航状态和局部交互状态，不重复维护任务真相。

**验收标准：**
- [ ] TypeScript strict、lint、单元与交互测试通过，不通过全局 `any` 或关闭检查规避错误。
- [ ] 使用键盘可完成主要表单操作；必填项、错误和焦点明确，颜色不是唯一状态编码。
- [ ] 1366×768 和 1920×1080 布局可用，主要任务不依赖水平滚动；科学图表可单独放大。
- [ ] 未配置、运行、失败和断线状态均有可操作的下一步，不出现无解释的空白卡片。
- [ ] 实验详情页可以刷新和直接访问；刷新不会重复提交任务。
- [ ] 至少一种真实大小的数据可视化完成原型性能验证；不能只用十个散点作为流畅性证据。

**交付物：** React 应用、设计系统实现、组件测试与视觉回归基准。

### S06｜实现前端打包与一键呼出浏览器

**目标：** 尽早验证用户真正安装到的程序，而不仅是开发服务器。

**开发内容：** 在发布 CI 中构建前端，并把产物纳入 wheel 与 sdist；新增 `opendpd gui`。实现服务就绪探测、端口选择、浏览器打开、实例复用、工作区锁、退出清理和 `--no-browser`。生产模式不使用 Vite dev server、热重载或双 Web worker。[R5][R6][R7]

**验收标准：**
- [ ] 从仓库之外、没有 Node.js 的干净环境安装 wheel，一条命令打开可用 GUI。
- [ ] sdist 同样包含可用静态资源，或在明确的构建阶段生成并验证；不能只有源码 checkout 能运行。
- [ ] 完成安装后断开外网，内置示例、字体、图标、图表、训练和导出仍可运行；可选下载必须显式提示。
- [ ] 端口占用、缺少默认浏览器、无桌面环境、重复启动、路径含空格/中文均有通过记录。
- [ ] 浏览器在健康检查成功之后打开；缺少静态文件或版本不匹配时显示诊断，不打开白屏。
- [ ] 三个平台分别完成人工真实浏览器启动测试；mock `webbrowser` 的测试只能证明调用逻辑。
- [ ] 基本访问会话和文件边界已生效，不能为快速 demo 暂时开放所有本地文件。

**交付物：** 可安装 alpha 包、一键启动器、平台 smoke test、最小完整演示。

**G0 放行：** 界面、API、工作区、启动和最小任务链打通；所有样例或模拟结果显著标识。此时尚不宣称完整科研流程可用。

### S07｜数据导入、Dataset Doctor 与可追溯预处理

**目标：** 外部用户能够判断自己的数据是否适用，并在不破坏原文件的情况下完成必要处理。

**开发内容：** 首版支持现有 OpenDPD 数据目录、CSV 和明确 dtype/shape 的 NumPy 数值数组；禁止含对象的数组反序列化。提供列映射、单位确认、输入输出配对、数据预览和 manifest 编辑。诊断 NaN/Inf、长度不匹配、可能削顶、时间错位、幅度范围、采样率/频带覆盖和元数据缺失。提供整数/分数延时与线性增益/相位校正的可审查路径；对歧义给出置信信息，不能自动断言错误。

**验收标准：**
- [ ] 内置数据和用户 CSV 可导入；错误列名、长度、I/Q 顺序和单位均可被解释并修正。
- [ ] 对已知延时、增益、异常值和削顶的合成夹具，检测结果达到夹具协议规定的容差；自然 PA 非线性不能被简单标为“数据坏了”。
- [ ] 诊断结果分为 error/warning/info；每项有证据与处理建议；元数据不充分时明确阻止不可靠的评估。
- [ ] 预处理先预览，再经用户确认创建新版本；原始数据哈希不变，处理参数与代码版本被记录。
- [ ] 数据划分在生成重叠训练帧之前执行；边界隔离至少覆盖上下文，避免相邻重叠帧跨 train/validation/test 泄漏。
- [ ] 用于拟合预处理参数的范围被记录；需要学习的全局归一化等参数不能从测试集拟合。测量对齐属于评估协议时单独声明，不与训练预处理混淆。
- [ ] 大文件增量读取；浏览器文件上传与服务端授权目录导入明确区分，不提供任意路径浏览。
- [ ] GUI、CLI 和 Python API 返回同一个版本的诊断报告和处理 manifest。

**交付物：** 导入适配器、Dataset Doctor、预处理与划分协议、数据页及故障夹具。

### S08｜版本化指标与评估证据体系

**目标：** 用户能知道每一个分数的算法、范围、单位及证据强度。

**开发内容：** 保留历史指标 profile；增加定义明确的通用 NMSE、带内误差与邻带功率比，不把通用频谱误差称为标准 EVM。将采样率、频带、窗函数、FFT/分段、归一化、聚合与有效样本范围放入版本化 profile。结果统一携带数据、模型、预处理、参考信号及评估来源。

**验收标准：**
- [ ] 历史 profile 在冻结夹具上保持旧数值；修复历史算法缺陷必须使用新版本或显式兼容模式。
- [ ] 至少包含零误差、已知复增益误差、已知噪声功率和已知频带功率的解析/独立参考测试。
- [ ] 区分邻带/主带的负 dBc 泄漏与主带/邻带的正 dB 抑制度；显示名称、公式和“更好方向”一致。
- [ ] 明确区分 pooled NMSE 与分段 dB 平均；EVM 的比例、百分数和 dB 表示不混用。
- [ ] 无有效参考、零参考能量、频带超出采集范围、空频带和非有限结果都有明确状态，不返回伪造的 0 或静默 NaN。
- [ ] GUI 默认展示证据标签和指标 profile；“代理模型结果”不能显示为“实测”。
- [ ] 显示抽稀、缩放或图表滤波不会改变正式指标结果。
- [ ] 不满足同一比较协议的结果不可自动生成胜负排名；允许并列展示但必须标出不兼容原因。

**交付物：** 指标注册与 profile、参考测试、证据 schema、结果卡片和解释文档。

### S09｜PA 训练闭环与实验管理

**目标：** 用户无需掌握全部命令参数，即可启动、监控和复现 PA 建模实验。

**开发内容：** 提供经过验证的参考 recipe，区分 smoke/demo 与 research。通过注册表生成基础参数与高级设置；展示数据版本、预处理、模型、设备和资源限制。实现实验列表、训练曲线、验证指标、日志、checkpoint 与参数对比。GUI 可导入和导出统一配置。

**验收标准：**
- [ ] 导入数据后可通过 GUI 完成一次真实 PA 训练，生成有效 checkpoint、日志与评估结果。
- [ ] 提交前检查数据、设备、模型参数和路径；不支持的设备不能静默切换，需用户确认替代方案。
- [ ] 快速演示和正式训练有明显区别；短训练结果不被包装成论文 benchmark。
- [ ] 同一 CPU 确定性小夹具经 GUI 与 CLI 执行，解析后的配置相同，输出满足冻结的数值容差。
- [ ] checkpoint 选择使用配置指定的 validation 指标，不使用 test 选择 epoch。
- [ ] 训练失败、取消、设备忙、队列等待和缺少 checkpoint 均有真实状态；不显示虚构进度。
- [ ] 日志支持分页/虚拟滚动，错误可定位到阶段，敏感路径和凭据不会进入对外诊断包。

**交付物：** PA 实验工作流、参考 recipe、监控页面、入口一致性测试。

### S10｜DPD 训练、信号生成与 surrogate 评估

**目标：** 清楚区分“生成预失真信号”和“已经验证线性化效果”。

**开发内容：** 实现从 PA checkpoint 选择到 DPD 训练、推理、评估的关联流程。显式展示 `x：目标输入`、`u = DPD(x)：预失真信号` 和 `y：PA 输出`；`y` 的来源必须是指定 surrogate 或关联的实际采集。验证 PA/DPD 配置、数据和模型版本兼容性；加入输入幅度分布与 surrogate 训练范围差异提示。

**验收标准：**
- [ ] 缺少兼容 PA surrogate 时，依赖它的 DPD recipe 无法启动，并说明解决办法。
- [ ] `run_dpd` 的输出明确标记为 PA 输入侧的预失真信号，不能当作线性化后的 PA 输出。
- [ ] 仿真链记录 PA checkpoint 哈希和评估 profile；换 surrogate 会产生新结果，不覆盖旧结果。
- [ ] 输入缩放、输出功率相关元数据与参考增益被记录；没有物理标定时不显示推算的 dBm/效率。
- [ ] 超出训练幅度覆盖的提示有可解释依据，但不承诺仅凭幅度诊断就能证明 surrogate 有效。
- [ ] baseline 与 DPD 的显示使用一致、可追溯的参考；不能通过分别归一化掩盖功率差异。
- [ ] 生成、保存并重新加载预失真数组后，样本顺序、dtype、幅度和元数据保持一致。

**交付物：** DPD 工作流、关联实验图谱、信号导出、仿真证据页和集成测试。

### S11｜结果比较、可视化与可复现实验包

**目标：** 用户能够解释结果、比较方法，并把实验带到另一台机器重现。

**开发内容：** 展示频谱、时域、AM-AM/AM-PM、训练曲线和配置差异；只在具备必要参考时显示有物理意义的星座/EVM 图。实现通用指标比较、CSV/JSON 导出、PNG/SVG 图像与 HTML/Markdown 报告。每份报告绑定数据和计算来源，不在前端重新计算指标。

**验收标准：**
- [ ] 同协议结果可比较；不同数据、工作点、surrogate、指标版本或执行语义的差异显式提示。
- [ ] 多个 trace 颜色、单位、参考和图例一致；隐藏图层不改变后台评估。
- [ ] 导出包含 resolved config、数据/模型/源码标识、环境、随机种子、指标 profile、结果和复现命令。
- [ ] 导出分为完整私有包与脱敏分享包；默认不把用户原始 PA 数据或敏感路径上传到外部。
- [ ] 数据不随包分享时记录缺失项和获取条件；校验和只能验证一致性，不能替代缺少的数据。
- [ ] 在新的工作区导入完整实验包，重算冻结 checkpoint 的指标，结果在数值容差内一致。
- [ ] 重新训练的可复现性单独报告；不能把“可重算已有输出”当作“训练结果完全相同”。
- [ ] 损坏、缺文件、哈希不一致和不支持版本的包拒绝运行，并返回具体诊断。

**交付物：** 结果工作台、导出器/导入器、报告模板、跨工作区复现测试。

**G1 放行：** 在真实数据上完成“导入—诊断—PA—DPD—评估—导出—CLI 复算”；至少一名未参与实现的内部用户独立完成该流程。标准 NR EVM 和自动仪器控制不属于此关卡前提。

### S12｜加强 benchmark、传统基线与数值回归

**目标：** 给 agent 和维护者建立客观裁判，避免代码变多但科学可信度下降。

**开发内容：** 复用现有 benchmark 产物及 provenance；分开固定 recipe 回归与资源预算下的方法比较。纳入可靠的 MP/GMP 基线、数值稳定性诊断、多个随机种子和明确的训练预算。限制测试集使用，记录所有提交与配置选择。保持普通 PR 的 CPU 回归与受控 GPU 全量验证分层。

**验收标准：**
- [ ] 每个发布结果都有可追踪命令、配置、输入、源码与产物哈希，不允许手改报告数值。
- [ ] 固定 checkpoint 的重评估属于确定性数值回归；重新训练属于统计回归，二者使用不同门槛。
- [ ] 正式随机模型对比至少包含 3 个预先指定种子，并报告每个种子及聚合结果；不宣称 3 个种子已充分证明普遍性。
- [ ] 同等参数量不被表述成同等计算成本；ILA/DLA 或其他训练路径差异被展示。
- [ ] PA surrogate 训练、DPD 优化和最终测试的数据关系可审计；评估 surrogate 的交叉验证不被称作物理验证。
- [ ] baseline 的拟合稳定性和调参预算被记录，不能用明显失效的传统实现制造提升。
- [ ] 超出预设回归范围时阻止自动发布；任何接受退化的决定都附原因和人工批准。
- [ ] 公共榜单提交不会直接在连接实验室数据、凭据或仪器网络的机器上执行。

**交付物：** 版本化 benchmark 协议、回归基线、CPU/GPU 验证流程、机器可读报告。

### S13｜性能、安全、无障碍与跨平台加固

**目标：** 从“功能能演示”变成“外部用户可稳定使用”。

**开发内容：** 从早期迭代起运行平台与浏览器测试，发布前完成压力、故障和安全验证。优化延迟加载、图表抽稀/聚合、数据缓存、日志分页、后台队列与内存占用。验证工作区迁移、备份恢复、端口/路径边界和恶意导入。具体性能指标见第 8 节。

**验收标准：**
- [ ] 第 8 节中 G2 必须项达到冻结阈值；所有报告记录机器、数据和软件版本。
- [ ] 打包后的 GUI 在支持矩阵上通过安装、训练 smoke、导出和退出测试；不是只测试 `npm run dev`。
- [ ] Chromium、Firefox、WebKit 测试覆盖主要旅程；另做真实 Safari 和系统默认浏览器启动验证。[R9]
- [ ] 长日志、大型数据、重复开关页面和训练期间操作，不造成无界内存或事件队列增长。
- [ ] 文件导入、结果预览和模型加载不执行任意 pickle/脚本；历史可信 checkpoint 兼容路径独立标注并测试。
- [ ] 恶意 Origin、跨站写入、路径穿越、符号链接绕过、XSS、ZIP 解压越界和超限请求有负向测试。
- [ ] 依赖、安全扫描与人工检查不存在未处理的发布阻断项；接受的残余风险记录在案。
- [ ] 无障碍自动检查和人工键盘路径检查通过；科学结果不依赖颜色单独表达。

**交付物：** 性能报告、平台证据、安全检查、迁移与恢复报告、发布候选包。

### S14｜外部试用、文档、贡献流程与首个公开版本

**目标：** 验证降低使用门槛，而不是只得到维护者自己的好评。

**开发内容：** 随功能编写 quickstart、数据准备、指标解释、添加模型、故障诊断和 GUI/CLI 对照教程。邀请至少三个独立外部小组试用，覆盖自己的数据、新方法和独立复现。用真实卡点驱动最后一轮修复。准备版本说明、兼容表、演示材料和可下载示例。

**验收标准：**
- [ ] 三个外部小组各完成至少一条真实任务；缺少的尝试和失败也计入报告。
- [ ] 一次不少于 5 名目标用户的入门测试中，至少 4 名无需维护者改代码即可完成规定示例流程；操作时间与训练等待分开统计。
- [ ] 安装、数据导入、首次训练和结果解释的阻塞问题都有已验证修复或明确降级路径。
- [ ] 文档中的主要命令在 CI 执行；示例 GUI 导出的配置可以通过 CLI 运行。
- [ ] 发布 wheel/sdist、校验和、版本说明和支持矩阵；演示素材来自同一候选版本。
- [ ] 数据共享、问题报告和匿名诊断包均经用户明确操作，不默认发送遥测或实验数据。
- [ ] 维护者完成发布批准；agent 不自动推送正式 release、数据或模型到外部平台。

**交付物：** 首个公开 Studio 版本、外部试用报告、教程与贡献指南、已排序的后续 backlog。

**G2 放行：** 端到端、可信数值、跨平台安装、安全、性能和外部可用性同时成立。若只完成漂亮界面或内部 demo，不得标记 G2 完成。

### S15｜限定范围的 Python 波形与标准评估 profile

**目标：** 在可验证的范围内减少 MATLAB 依赖，而不是重写整个通信工具箱。

**开发内容：** 先选择你们最常用的一种明确波形配置，固定标准版本、参考资源、采样率、同步、均衡、测量窗口与频带定义。优先评估可复用实现，记录支持范围及许可证。保留 MATLAB 或仪器链作为独立交叉验证后端，形成参考 I/Q、参考符号与预期结果包。GUI 只展示已经验证的 profile。

**验收标准：**
- [ ] 每个 profile 有明确的标准版本/条款引用、支持范围和不支持情况，不使用泛化的“3GPP compliant”宣传。
- [ ] 无需 MATLAB 的用户可运行发布的参考向量及评估；交叉验证时使用 MATLAB 不等于用户运行必须依赖 MATLAB。
- [ ] 理想信号、已知噪声、失真、定时/频偏扰动和错误配置均有测试。
- [ ] 同一数字参考信号上的初始比对目标：邻带比值误差不超过 0.1 dB，RMS EVM 绝对差不超过 0.05 个百分点；测量下限和参考不确定度另行记录，低于有效范围不强行评分。
- [ ] profile 的误差预算在开始模型比较前批准；不允许为适配某个模型结果事后放宽。
- [ ] 不符合 profile 的任意 I/Q 输入返回“不适用/缺少参考”，不得回退成另一个定义而沿用标准名称。
- [ ] 原有历史指标和旧论文复现不受影响。

**交付物：** 第一个经交叉验证的波形/评估 profile、参考向量、GUI 选项和验证报告。后续配置以相同流程增量扩展。

### S16｜真实 PA 数据闭环与受控仪器适配

**目标：** 把仿真改进与真实线性化效果连起来，同时控制硬件风险和维护范围。

**开发内容：** 先支持导入用户手工采集的 DPD 前/后输出及测量元数据；之后针对一套现有实验室配置实现仪器 adapter、mock/dry-run 和安全控制。记录 PA、波形、采集链、采样率、驱动、输出功率、增益、校准及测量时间。定义预失真信号与采集结果之间的关联。

**验收标准：**
- [ ] 没有仪器连接也可通过手工采集导入完成 `dpd_measured` 流程，并明确标注为用户提供、尚未独立核验的测量证据。
- [ ] 实测图表可追溯到原始采集文件、信号哈希和测量条件；仅上传一张频谱截图不构成可重算证据。
- [ ] 比较时输出功率、波形和工作点条件一致或显式报告差异，不通过降低驱动伪装为 DPD 改善。
- [ ] 自动控制路径默认 RF 输出关闭，必须人工 arm；功率/幅度限制、超时、失联、异常停止与 interlock fail-closed。
- [ ] 普通 CI 只运行仪器 mock；真实输出仅在获准实验环境执行，agent 无权自行解除限制。
- [ ] 至少一套目标仪器链完成“导出—下载—采集—导入—评估”的人工监督试验。
- [ ] 仿真与实测分开显示，差异可以分析但不隐藏；一次硬件成功不被表述成支持全部仪器。

**交付物：** 实测 schema、人工采集工作流、一个受控 adapter、硬件测试记录和操作规程。

### S17｜多工况、泛化与快速适应基准

**目标：** 从单一工作点的最优分数，发展为可验证的工程适应能力。

**开发内容：** 收集多个功率点、采集批次或波形条件的数据；温度/负载变化按可用资源逐步加入。分开零更新泛化、有限新数据适应和完整重训三种任务。GUI 展示条件矩阵、适应成本和性能—资源关系。

**验收标准：**
- [ ] 至少一个真实变化维度含 3 个以上条件，并包含独立采集批次；不能只把同一 capture 随机拆成“不同工况”。
- [ ] 训练、适应和测试条件预先划分，held-out 数据不参与超参数选择。
- [ ] 零更新、少量适应和完整重训分别报告，记录新数据量、训练时间、计算设备和达到目标的条件。
- [ ] 数据/测量重复与训练种子分开统计；单一 PA 的结果不声称跨器件普适。
- [ ] GUI 能过滤工况和显示失败条件，不只展示平均最好的一列。
- [ ] 至少一个外部或非原实现者复算公开子集结果。

**交付物：** 多工况 data card、评估协议、参考方法、鲁棒性页面和结果包。

### S18｜流式执行、上下文与延迟语义

**目标：** 明确离线分段模型如何变成可解释、可验证的流式模型。

**开发内容：** 定义 `reset/state/step/chunk/flush` 接口；说明 lookahead、warm-up、padding、状态重置和有效输出范围。先支持一个目标模型，再逐步覆盖其他模型。将任何会改变历史边界行为的流式版本注册为显式新变体，保留旧离线行为。

**验收标准：**
- [ ] 同一流式语义下，整流参考与多种 chunk 大小的输出在有效范围内满足数值容差。
- [ ] 状态跨 chunk 正确传递；不能用循环移位把当前块尾部错误连接到块首。
- [ ] lookahead 以样本数及对应时间明确展示；它不等同于测得的实现延迟。
- [ ] 全部缓存、warm-up、flush 和尾部处理纳入输出对齐测试。
- [ ] 因果模型不读取未声明的未来样本；有 lookahead 的模型必须报告缓冲代价。
- [ ] 新流式变体重新评估性能；不能直接继承旧分段 benchmark 分数。

**交付物：** 流式执行契约、一个经过验证的模型变体、chunk 一致性测试与执行语义报告。

### S19｜位精确参考与部署导出

**目标：** 建立从浮点研究代码到定点部署实现之间的可验证桥梁。

**开发内容：** 对一个已通过 S18 的模型，固定权重、激活、状态和累加器位宽，定义舍入、饱和、缩放及非线性近似。生成 bit-exact 软件参考、golden vectors 和目标格式导出。先针对一个后端验证，不同时承诺 ONNX/HLS/RTL 全覆盖。

**验收标准：**
- [ ] 定点规范覆盖全部算子和状态，不仅是权重量化。
- [ ] 正常、极值、溢出、全零、状态重置和长序列测试都有 golden vectors。
- [ ] 目标实现与软件参考在定义的接口上逐位一致；不一致必须定位，而不是只比较最终 ACLR。
- [ ] 浮点到定点的质量损失、MAC/采样、状态/权重存储和执行假设分别报告。
- [ ] 理论稀疏、实测执行时间、综合估计和实测功耗使用不同标签，不由参数量直接推断能耗。
- [ ] GUI 能导出部署包并显示支持范围；不支持导出的模型有明确解释而不是损坏的按钮。

**交付物：** 定点规范、bit-exact 参考、一个验证过的导出后端、golden vectors 和 GUI 导出流程。

### S20｜社区提交、复核与版本化 leaderboard

**目标：** 将被外部用户实际使用的协议发展为公共基准，而不是先建榜再等待提交者。

**开发内容：** 建立配置/模型/结果提交流程、数据与模型 card、许可证检查、引用方式和证据分级。榜单按任务、数据、工作点、指标版本、执行语义和资源预算分组；允许“不可比较”状态。模型和数据的外部发布须逐项获授权。建立协议变更、结果纠错、利益冲突和申诉规则。

**验收标准：**
- [ ] 至少三个来自非核心维护者的完整提交通过复核，且至少两个由非原作者独立复算。
- [ ] PA 建模、surrogate DPD、实测 DPD 分榜；标准、鲁棒性和部署赛道只在对应步骤通过后启用。
- [ ] 每个条目可追溯到可获取的产物与协议版本；不可公开的数据限制被明确标注，不冒充公开可复现。
- [ ] 同一版本的协议和测试集固定；新版本不覆盖旧榜，结果撤回和更正保留历史。
- [ ] 排名展示不确定性、资源和失败条件，不仅按一个 ACLR 数字排序。
- [ ] 外部方法和维护者自己的方法使用相同审核规则；至少一名外部研究者参与协议审阅。
- [ ] 提交中的代码和模型在隔离验证环境执行，不能直接进入实验室 self-hosted runner。
- [ ] 没有达到外部提交门槛时，页面称为“参考 benchmark”，不自称社区标准。

**交付物：** 版本化榜单、提交模板、复核工具、公开协议、治理与引用文档。

---

## 7. 接口、结果与复现的最低规范

### 7.1 首版 API 面

下表是建议接口契约，命名可在 S01 调整；S01 通过后，破坏性调整需要版本与迁移说明。

| 方法与路径 | 用途 | 必须具备的行为 |
|---|---|---|
| `GET /healthz` | 存活探测 | 不触发 GPU 初始化；不泄露环境和路径。 |
| `GET /readyz` | 启动就绪 | 检查静态资源、工作区和必需服务；返回明确未就绪原因。 |
| `GET /api/v1/system/capabilities` | 设备和功能 | 设备“检测到”与“通过该 recipe 测试”分开。 |
| `GET /api/v1/models` | 模型与能力 | 来自同一注册表，包含参数 schema、设备及执行语义。 |
| `POST /api/v1/datasets/import` | 导入数据 | 有限大小、明确来源；大文件处理返回任务标识。 |
| `POST /api/v1/datasets/{id}/diagnostics` | 数据检查 | 返回 job/run 标识，长处理不阻塞请求。 |
| `POST /api/v1/experiments/validate` | 预验证配置 | 返回 resolved config、错误和警告；不启动训练。 |
| `POST /api/v1/runs` | 启动任务 | 幂等、持久化、资源限制、可追溯配置。 |
| `GET /api/v1/runs/{id}` | 状态与摘要 | 从持久化真相读取，包含关联实验与产物状态。 |
| `GET /api/v1/runs/{id}/events` | SSE 进度 | 事件 ID、重放边界、心跳及重同步机制。 |
| `POST /api/v1/runs/{id}/cancel` | 请求取消 | 幂等；返回“已请求”，不提前假称已停止。 |
| `GET /api/v1/results/{id}` | 正式结果 | 指标、profile、来源、有效性与不确定信息。 |
| `GET /api/v1/artifacts/{id}` | 下载产物 | 通过登记 ID 解析路径，不接受任意文件路径。 |
| `POST /api/v1/exports` | 导出实验 | 明确私有包/分享包、包含范围和脱敏状态。 |

首版单用户本地模式只需要一个应用服务和一个 supervisor，不引入 Kubernetes、Redis 或 Celery 集群。未来确有多机需求时，再用保持接口不变的 executor adapter 扩展。

### 7.2 配置规范

所有运行保存**原始用户配置**与**归一化后的最终配置**；最终配置必须包含所有影响结果的默认值。推荐默认导出 JSON，避免前期维护两套不一致的配置语法。

以下仅为契约示例，字段由 S01 最终固化，不代表当前已存在可运行命令：

```json
{
  "schema_version": 1,
  "task": "train_pa",
  "recipe_id": "pa-gru-smoke-v1",
  "dataset": {
    "id": "example-pa-v1",
    "preprocessing_version": "raw-v1",
    "split_version": "capture-split-v1"
  },
  "model": {
    "key": "gru",
    "parameters": {"hidden_size": 28, "num_layers": 1}
  },
  "training": {
    "epochs": 3,
    "batch_size": 64,
    "learning_rate": 0.005,
    "seed": 0
  },
  "evaluation": {
    "profile_id": "legacy-opendpd-v1",
    "evidence_type": "pa_modeling"
  },
  "execution": {"device": "cpu"}
}
```

上述 `epochs: 3` 是 smoke 示例，不是研究基准。用户选择的 recipe 需同时展示其用途及限制。

### 7.3 正式结果必须包含的字段

| 类别 | 最低内容 |
|---|---|
| 身份 | result/run/experiment ID、schema 版本、生成时间 |
| 数据 | dataset ID、原始与处理后哈希、划分及处理版本 |
| 模型 | PA/DPD 模型标识、权重哈希、参数、执行语义 |
| 评估 | evidence type、metric profile、有效样本范围、参考信号和增益规则 |
| 数值 | 指标名、值、单位、更好方向、有效性及原因 |
| 运行 | 软件版本、源码标识、设备、种子、数值模式、起止状态 |
| 证据 | 输入/输出文件、日志、checkpoint、来源与校验和 |
| 限制 | 未知元数据、未验证设备、surrogate 限制、测量下限和缺失项 |

`null`/不适用/失败/无穷大必须有明确含义。禁止用零填充缺失指标；JSON 中不能写入不合法的 NaN 或 Infinity 字面量。图表、报告与 API 展示同一份已经记录的正式结果。

### 7.4 持久化与事件一致性

SQLite 是任务状态和结构化事件的权威记录；状态变更与对应事件尽量在同一事务提交。`events.jsonl` 是可重建的导出/审计产物，不与数据库争做第二个任务真相来源。高容量 stdout/stderr 可存文件，但只引用已登记的路径。

worker 完成后，supervisor 验证产物完整性，再提交终态。恢复时通过任务身份、创建时间、退出信息和产物记录核对，不仅依据一个可能被复用的 PID。用户原始数据被外部修改时需重新校验并创建版本，不能继续沿用旧哈希。

### 7.5 本地访问安全的具体要求

默认只有回环网络可访问，但仍需防止恶意网页访问本地端口。建议使用一次性 bootstrap 凭据交换受限本地会话；凭据不写入普通日志或分享包，交换后移除 URL 中的敏感内容。校验 Host、Origin 和写请求的防跨站凭据；CORS 不使用任意源通配。

文件访问仅允许明确授权的工作区或导入根目录，且检查解析后的真实路径。用户模型导入不加载任意对象或自动下载执行代码；不可信第三方模型应进入独立审查路径。常规 UI 不暴露 shell、Python REPL 或自动执行安装脚本。

首版远程使用仅支持用户自行建立的受控 SSH 隧道，服务仍绑定回环。不得因为“可以访问浏览器”就默认开放 `0.0.0.0`；直接网络暴露、共享实验室账户和多用户服务属于独立安全设计范围。

---

## 8. 非功能验收：性能、平台与测试

### 8.1 测量协议

以下数字是**建议冻结的验收目标**。S00/S01 先测量并确认可行性；批准之后，修改阈值必须有性能报告、原因和人工批准，不能由 agent 自动调整。

参考机器类别：4 个或以上物理 CPU 核、16 GB RAM、本地 SSD、1920×1080 显示环境；记录具体 CPU、操作系统、Python、浏览器和包版本。GPU 性能另用固定测试机，不能把云 CI 的波动当作算法回归。

| 测试数据 | 内容 | 目的 |
|---|---|---|
| Tiny | 固定种子的短合成 PA 对及诊断异常夹具 | 普通 PR 的快速功能、状态和数值测试；明确为合成。 |
| Standard | `10^6` 和 `10^7` 组配对复数样本 | 真实大小的数据导入、诊断、图表和训练流程。 |
| Stress | `10^8` 组配对复数样本，float32 I/Q 二进制存储 | 检查增量读取和内存上界，不要求每个 PR 都运行。 |
| History | 至少 1,000 条实验摘要、50,000 行任务日志 | 分页、搜索、虚拟列表和长期使用。 |

配对样本指输入和输出各一组 I/Q；Stress 的原始数组约为 1.6 GB，依据 `10^8 × 2路 × 2分量 × 4字节` 计算，不包含元数据和其他副本。

启动至少测量 20 次，分别报告冷启动与缓存后的启动；交互/API 指标至少测量 100 次并报告 p50/p95。训练可视化开销使用同一配置、设备和资源限制做至少 3 组配对实验，报告中位数及波动范围。

### 8.2 G2 必须通过的性能目标

| 指标 | 初始验收目标 | 边界说明 |
|---|---|---|
| 命令到服务可用 | 已安装环境中 p95 ≤ 10 秒 | 不含安装、依赖下载和大型工作区迁移；启动不加载大数据或初始化全部 GPU。 |
| 服务就绪到首页可交互 | p95 ≤ 2 秒 | 静态资源从本机加载，使用参考浏览器和工作区。 |
| 常规界面操作反馈 | p95 ≤ 200 ms | 按钮、标签、配置展开等立即反馈；不把完整 FFT 算进点击反馈。 |
| 状态/元数据 API | p95 ≤ 250 ms | 排除大文件和计算任务；训练运行期间也测量。 |
| 已缓存图表缩放/切换 | p95 ≤ 300 ms | 按视口和像素预算供数，不能把全量原始数组发往浏览器。 |
| 训练状态可见延迟 | ≤ 2 秒 | 在约定日志/指标更新频率下；异常长算子需解释最后心跳时间。 |
| GUI 对训练的额外开销 | 配对测试中位数 ≤ 5% | 同等数值算法、线程预算和指标计算频率；报告波动，不隐瞒异常运行。 |
| 控制层内存增长 | 30 分钟稳定负载后，Web/UI 不出现随累计事件数线性增长 | 预热后最近 20 分钟净增长目标 ≤ 100 MB；worker 模型内存单独记录。 |
| Stress 导入额外内存 | 解析/转换进程额外 RSS 峰值 ≤ 1 GB | 不计操作系统文件缓存；不将整个 CSV/数组重复复制到 RAM。 |
| 取消请求反馈 | UI ≤ 1 秒进入 cancel_requested | 合作式停止默认等待不超过 30 秒；超时明确进入强制终止和清理。 |
| 首版日志/历史浏览 | 50,000 行日志与 1,000 个 run 可正常滚动、搜索和分页 | 禁止全部挂载为 DOM 元素。 |
| 离线使用 | 安装完成且数据已在本机时，主要旅程无需外网 | 无外部字体/CDN/默认遥测；可选数据下载单独处理。 |

若某个指标在特定支持平台无法达到，必须优化、缩小正式支持范围，或通过 ADR 修改承诺；不能仅把失败测试跳过。

### 8.3 数值测试与训练回归的区别

**确定性算法测试：** 对冻结输入和指定精度，采用预先批准的逐元素/指标容差；包括解析边界值和独立参考实现。不能只拿同一个待测函数生成 expected values。

**已有 checkpoint 的结果复算：** 固定数据、预处理、执行语义及评估 profile，验证产物与指标。对不同设备产生的数值差异使用记录的容差，而不是默认跨平台位级相同。

**重新训练的结果回归：** 采用固定种子集合、资源预算和协议，报告分布及失败情况。阈值依据基线波动批准；不能要求每次优化都得到同一精确分数，也不能只保留最好的那次。

**真实测量验证：** 在数字误差以外记录仪器校准、测量不确定度、工作点与重复测量。软件复算一致不能证明测量链本身正确。

### 8.4 测试分层与触发

| 层级 | 内容 | 建议触发 |
|---|---|---|
| L0：静态与单元 | Python/TS 检查、schema、状态机、指标 golden、组件交互 | 每个 PR |
| L1：集成 | Tiny CPU 训练、数据诊断、取消、事件重连、GUI/CLI 一致性 | 每个相关 PR |
| L2：已打包 E2E | wheel 安装、真实服务、浏览器旅程、无 Node.js、离线资源 | 相关 PR 和每个发布候选 |
| L3：平台/压力 | 多 OS、多浏览器、Stress 数据、长日志、服务崩溃、迁移恢复 | 定期任务和发布候选 |
| L4：GPU/统计回归 | 指定 commit 的 CUDA/MPS 能力、多种子 benchmark | 人工批准后、算法变更和正式发布 |
| L5：硬件 | 真实仪器、功率限制、采集闭环 | 人工监督的授权实验，不在公开 PR 自动触发 |

Playwright 的 projects 机制用于复用多浏览器/环境测试配置，但不能替代实际操作系统的浏览器自动启动与真实仪器测试。[R9]

---

## 9. Coding agent 的任务组织与持续迭代

### 9.1 角色与权限

| 角色 | 可承担内容 | 不得自行决定的事项 |
|---|---|---|
| 产品/科研负责人 | 优先级、用户旅程、指标和协议批准、发布 | 不以主观截图替代测试证据。 |
| 核心/后端 agent | 配置、运行时、API、数据和数值测试实现 | 不修改科学语义、golden 或基准容差来让结果通过。 |
| 前端 agent | 设计系统、页面、交互、图表与 UI 测试 | 不在前端另写正式指标；不增加未批准的产品范围。 |
| QA/复现 agent | 独立测试、故障注入、复现实验和证据汇总 | 不代替实现方“顺手修正”期望答案。 |
| 人工 reviewer | 审核高风险逻辑与集成决策 | 正式协议、发布、实测与敏感权限必须有人负责。 |

同一个 agent 可以承担不同阶段，但负责实现与验证时必须使用独立测试依据；多个 agent 的一致意见不能替代数值参考和用户试用。

### 9.2 日常循环

每日扫描 CI、文档漂移、已报告复现错误和批准队列；存在有依据的问题时生成候选 issue，没有新证据时允许不产出。实现任务从已批准队列领取，每个 agent 同时只处理一个核心目标。

每日可以提交小 PR，但初始限制为**最多 2 个待人工审核的 agent PR**。达到上限、缺少验收数据、超出资源预算或遇到协议歧义时，停止新实现，提交阻塞说明，不自行扩大范围。

每周审阅外部用户阻塞、benchmark 回归和维护成本，从中调整 backlog。论文扫描和开放式 brainstorm 的产物首先是带证据的提案，不自动变成新模型合并。

这是一套拟开发/配置的工作机制；本文没有创建任何定时自动化任务。

### 9.3 高风险变更

以下事项必须单独成 PR，并获得相应人工批准：指标定义和符号、数据划分、评估样本范围、归一化、checkpoint 选择、默认训练 recipe、模型因果性、定点规则、授权边界、CI 权限、数据/模型外部发布和仪器控制。

GitHub 官方的安全指引强调最小权限及不可信代码执行风险；公共 PR、issue 和附件不能直接获得实验室 GPU 主机、凭据和仪器网络的访问权限。[R10]

受控 GPU 验证必须绑定**被批准的确切 commit**；验证通过后若代码改变，原批准不自动沿用。使用隔离环境、受限凭据和网络，不以“跑在容器里”本身作为完整安全证明。

### 9.4 每个任务的 issue 模板

```markdown
## Task
Sxx.y — 一个可独立审阅的目标

## User problem
谁在什么流程中遇到什么问题？证据是什么？

## Goal
完成后用户能做什么？

## Dependencies
依赖的步骤、接口版本、数据夹具或前置 PR。

## Scope
明确的开发内容和允许修改的模块。

## Non-goals
本任务明确不做什么。

## Acceptance criteria
- [ ] 可自动检查的行为与数值门槛。
- [ ] 至少一个正常路径和一个失败路径。
- [ ] 必要的 GUI / CLI / API 一致性要求。
- [ ] 文档、兼容性和平台要求。

## Evidence required
测试命令、环境、日志、截图/trace、结果与产物路径。

## Risk / approval
是否涉及指标、数据、权限、发布或硬件？由谁批准？

## Rollback
如何撤回代码或恢复数据；不能用删除失败证据作为回滚。
```

### 9.5 每个 PR 的完成定义

一个任务只有在代码、契约、测试、文档和证据一起完成时才算完成。PR 必须说明解决的用户问题、改动范围、未完成项和风险；截图只用于 UI 检查，不能替代真实后端结果。

不允许无关格式化、顺手更换依赖框架、修改随机种子求更好分数，或为了降低 review 压力把不同风险事项揉进一个大 PR。纯重构必须证明对冻结输入的行为保持一致；无法保持时应作为行为变更审阅。

---

## 10. 推荐的首批执行任务

不从“把所有参数搬进 GUI”开始。首先创建以下任务，并按依赖执行：

| 顺序 | 首批任务 | 第一份完成证据 |
|---|---|---|
| 1 | S00：固定现状和兼容测试 | 基线报告、真实小型训练日志、风险清单。 |
| 2 | S01：冻结最小 config/run/result 契约与用户旅程 | schema、mock、主页面规格通过审阅。 |
| 3A | S02：显式配置与独立工作区入口 | 旧 CLI 不坏；新配置不改写全局参数。 |
| 3B | S05：React 设计系统与 mock 页面 | 可点击的完整旅程、状态与组件测试。 |
| 4 | S03/S04：一个独立 Tiny 任务，经 API 提交并持久化 | 真实 worker、日志、取消、状态重连与安全测试。 |
| 5 | S06：发布包与一键启动 | 无 Node.js 环境安装，默认浏览器自动打开真实应用。 |
| 6 | S07/S08：数据诊断与明确的历史指标 profile | 导入真实 PA 数据，检查并展示有来源的指标。 |
| 7 | S09–S11：完成一个受限的真实 PA/DPD recipe | GUI 运行并导出，另一工作区通过 CLI 复算。 |

3A 与 3B 可以并行。4 和 5 应切成可独立审阅的小 PR；用户可安装的最小产品应尽早出现，但在 G1/G2 通过前明确标为 alpha。

**首个完整演示的范围固定为：一个数据集、一套参考 PA/DPD recipe、一种经过验证的计算设备、一个版本化评估 profile、一份可复现实验包。**跑通之后再扩展模型、设备和页面。

---

## 11. 主要风险与应对

| 风险 | 早期信号 | 应对与门槛 |
|---|---|---|
| GUI 与后端分叉 | 前端出现自己的指标计算或默认参数 | 所有正式计算走共用服务；配置与结果使用契约测试。 |
| agent 功能堆积 | PR 数增长但用户闭环未完成 | 限制在制任务与待审 PR；按用户旅程而不是代码量验收。 |
| Web 服务器被训练阻塞 | 点击和状态请求随训练长时间冻结 | 独立 worker、事件节流、固定资源预算、压力测试。 |
| 界面漂亮但结果不可信 | 隐藏 profile、混用实测与 surrogate | 证据标签和元数据作为结果 schema 必填项。 |
| 打包失败 | 只有源码环境和 Node.js 存在时可用 | G0 即测试 wheel/sdist，发布前跑无 Node.js/离线安装后场景。 |
| 大数据使浏览器卡死 | 整段 I/Q 或全量日志进入前端 | 后端聚合、按视口取数、虚拟列表、内存压力测试。 |
| 训练可复现性被过度承诺 | 不同 GPU 有差异就修改 expected | 区分确定性复算与统计训练回归，报告环境和容差。 |
| 本地服务不安全 | 任意网页能写接口、任意路径可读 | 会话、Origin/Host、防跨站、路径沙箱和负向测试。 |
| 硬件集成范围失控 | 同时开发多家仪器驱动 | 先手工采集导入，再一个 adapter；每个扩展需独立需求。 |
| 标准功能拖延首版 | 为“完整 3GPP”迟迟不能发布 | S15 独立交付一个限定 profile，不阻塞 G2。 |
| 榜单缺少社区参与 | 只有维护者自己的模型和数据 | 先外部试用/复核，达不到门槛只发布参考 benchmark。 |

---

## 12. 产品与社区效果的验收

技术测试通过不等于影响力已经扩大。G2 发布后，应在用户同意的前提下通过试用记录、公开反馈和自愿报告追踪以下指标；不要求安装遥测。

| 指标 | 统计方法 | 初始成功信号 |
|---|---|---|
| 首次成功率 | 完成指定旅程的人数 / 实际尝试人数 | 5 人入门试用中至少 4 人独立完成；持续公布失败原因。 |
| 自有数据使用 | 外部小组的真实导入与结果报告 | 至少 3 个独立小组完成可核对实验。 |
| 复现成功 | 不同工作区/不同操作者的复算记录 | 公开参考结果有外部复算证据。 |
| 贡献门槛 | 从添加模型教程到提交有效 PR 的阻塞项 | 非核心维护者无需改内部流程即可完成首个贡献。 |
| 维护成本 | 每次成功实验需要的支持轮数/人工时间 | 随版本下降；相同问题转化为诊断、界面提示或文档测试。 |
| 科学可信度 | 不可比较结果、错误指标、缺证据报告的发生与修复 | 不隐藏错误；更正可追踪；不能靠减少报告错误来优化指标。 |
| 持续使用 | 试用者是否完成第二个真实实验 | 不只停留在一次演示或下载。 |

Stars、下载量、截图传播和论文引用可作为补充指标，但不替代上述产品和科学验收。

---

## 13. 项目负责人最终检查表

### 公开发布前

- [ ] `opendpd gui` 在受支持平台自动打开可用浏览器界面，最终用户不需要 Node.js。
- [ ] 普通核心安装与旧脚本可继续使用；支持范围和弃用变更有明确说明。
- [ ] 数据导入、诊断、PA/DPD 训练、评估与实验导出构成真实闭环。
- [ ] GUI、CLI 和 Python API 共用配置与正式计算，入口一致性有测试。
- [ ] 原始数据不可被静默覆盖，预处理、划分和指标版本可追溯。
- [ ] surrogate、PA 建模和实测结果明确区分；没有标准参考时不显示伪标准 EVM。
- [ ] 任务取消、浏览器断开、服务退出、崩溃和迁移均有定义及测试。
- [ ] 性能、安全、平台和离线资源测试通过，失败项不被隐藏。
- [ ] 外部试用不是只有内部开发者演示；文档命令与真实版本一致。
- [ ] 发布、上传、协议修改和真实 RF 输出均保留人工授权门槛。

### 下一阶段投资判断

优先投入能减少外部用户阻塞、提高结果可信度、增加独立复现或降低维护成本的任务。只有这些基础已经成立，再扩大标准覆盖、仪器范围、部署后端和公共榜单。

**完成本计划的核心标志不是“OpenDPD 有了一个 GUI”，而是外部用户能够通过 GUI 独立完成、理解并复现可信的 PA/DPD 实验。**

---

## 14. 来源与技术依据

来源检索日期：2026-09-06。仓库事实固定到所列 commit；官方库文档会更新，执行 S00/S01 时应记录实际采用的依赖版本和文档版本。本文的任务排序、界面设计、验收阈值和治理规则是本计划的设计决策，不是这些来源已经验证的项目成果。

**[R1] OpenDPD 基线 commit。**  
`https://github.com/lab-emi/OpenDPD/commit/7426bbf8a47624b59bd7f045a86641b403023f3c`

**[R2] OpenDPD 包元数据与命令入口。**  
`https://github.com/lab-emi/OpenDPD/blob/7426bbf8a47624b59bd7f045a86641b403023f3c/pyproject.toml`

**[R3] OpenDPD Python API 及参数构造。**  
`https://github.com/lab-emi/OpenDPD/blob/7426bbf8a47624b59bd7f045a86641b403023f3c/opendpd/api.py`

**[R4] OpenDPD benchmark 的定义、方法、局限与来源记录。**  
`https://github.com/lab-emi/OpenDPD/blob/7426bbf8a47624b59bd7f045a86641b403023f3c/benchmark/benchmark_report.md`

**[R5] Vite：生产构建。**  
`https://vite.dev/guide/build`

**[R6] FastAPI：静态资源服务。**  
`https://fastapi.tiangolo.com/tutorial/static-files/`

**[R7] Python：webbrowser 接口和失败行为。**  
`https://docs.python.org/3/library/webbrowser.html`

**[R8] FastAPI：Background Tasks 与重计算边界。**  
`https://fastapi.tiangolo.com/tutorial/background-tasks/`

**[R9] Playwright：多浏览器与项目测试配置。**  
`https://playwright.dev/docs/test-projects`

**[R10] GitHub Actions：安全使用、最小权限与不可信代码风险。**  
`https://docs.github.com/en/actions/reference/security/secure-use`
